/**
 * Icon set `bold-24` — the outline glyphs drawn with a heavier stroke (2.75, from the registry).
 * Same mapping as outline-24; only the set metadata differs, and `<Icon>` applies it.
 */
export { glyphs } from './outline-24.ts';
