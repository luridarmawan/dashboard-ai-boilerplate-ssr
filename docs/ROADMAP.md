# Roadmap Pengembangan

| | |
|---|---|
| **Sumber kebutuhan** | [`PRD.md`](./PRD.md) — kebutuhan fungsional (FR-A…FR-R), kriteria terima §8, rencana rilis §9 |
| **Dokumen terkait** | [`THEMES.md`](./THEMES.md) — tema bawaan (sudah selesai, lihat M2) |
| **Asumsi tim** | **2 developer** yang bisa mengerjakan backend maupun frontend, penuh waktu |
| **Estimasi** | Dalam **person-week (pw)** dan rentang, bukan tanggal. Kalibrasi ulang setelah M0 selesai — M0 adalah pengukur kecepatan tim yang sesungguhnya |
| **Total kasar** | 31–42 pw ≈ **16–22 minggu kalender** dengan 2 developer |

Estimasi di sini adalah dugaan terdidik, bukan komitmen. Yang bisa dipegang adalah **urutannya** dan **gate keluarnya** — keduanya diturunkan dari risiko, bukan dari kenyamanan.

---

## 1. Prinsip penjadwalan

Tiga aturan yang menentukan urutan di bawah, dan layak diperdebatkan sekarang — bukan di tengah jalan.

**1. Risiko terbesar dibuktikan lebih dulu, sebelum ada yang dibangun di atasnya.**
Dua taruhan teknis terbesar proyek ini adalah (a) skema Drizzle netral-dialect lewat codegen, dan (b) kontrak modul yang sanggup dipakai dari repositori lain. Keduanya menyentuh setiap fitur setelahnya. Kalau (a) gagal di minggu ke-4, kita mengubah cakupan. Kalau gagal di minggu ke-20, kita menulis ulang. Karena itu **keduanya masuk M0**, bahkan sebelum autentikasi.

**2. Kontrak dikunci sebelum pemakainya banyak.**
Kontrak modul, amplop respons API, dan token tema semuanya lebih murah diubah saat ada satu pemakai daripada saat ada tiga puluh. Urutan M0 → M1 → M2 mengikuti ini: kontrak, lalu domain, lalu permukaan.

**3. Penjaga CI dinyalakan bertahap, bukan di akhir.**
Kriteria terima §8 bukan pemeriksaan sekali di ujung. Setiap milestone menyalakan pemeriksaan otomatisnya sendiri (§6 di bawah), sehingga regresi ketahuan pada hari ia masuk, bukan empat bulan kemudian.

---

## 2. Peta ketergantungan

```
                          ┌──────────────────────────┐
                          │  M0  Fondasi & Kontrak   │  ← gerbang risiko
                          │  db codegen · modul      │
                          └────────────┬─────────────┘
                                       │
                    ┌──────────────────┴──────────────────┐
                    │                                     │
          ┌─────────▼─────────┐                 ┌─────────▼─────────┐
          │ M1  Identitas     │                 │ M2  UI/Tema/Layout│   ← bisa paralel
          │ auth·tenant·RBAC  │                 │ komponen·i18n     │      (2 developer)
          └─────────┬─────────┘                 └─────────┬─────────┘
                    └──────────────────┬──────────────────┘
                                       │
                          ┌────────────▼─────────────┐
                          │ M3  Konfigurasi & Kontrak│
                          │ config·OpenAPI·route baku│
                          └────────────┬─────────────┘
                                       │
                    ┌──────────────────┴──────────────────┐
          ┌─────────▼─────────┐                 ┌─────────▼─────────┐
          │ M4  Example       │                 │ M5  Modul AI      │   ← bisa paralel
          │ landing·SEO·email │                 │ chat·log·stream   │
          └─────────┬─────────┘                 └─────────┬─────────┘
                    └──────────────────┬──────────────────┘
                                       │
                          ┌────────────▼─────────────┐
                          │ M6  Modul lintas repo    │  ← gerbang janji produk
                          │ modgen·starter·CI guard  │
                          └────────────┬─────────────┘
                                       │
                          ┌────────────▼─────────────┐
                          │ M7  Pengerasan & Operasi │
                          └──────────────────────────┘
```

M1∥M2 dan M4∥M5 adalah satu-satunya paralelisasi yang aman. Memaksakan lebih dari itu berarti bekerja di atas kontrak yang belum stabil.

---

## 3. Milestone

Setiap milestone punya **gate keluar** — pernyataan yang bisa dijawab ya/tidak, bukan "kira-kira sudah". Milestone tidak dinyatakan selesai sebelum seluruh gate-nya hijau.

**Invarian cakupan:** M0–M7 memuat kebutuhan **P0 saja**. Setiap item P1/P2 hidup di §8, dan hanya di sana. Sebuah item yang muncul di keduanya bukan penekanan — itu sinyal cakupan sedang membengkak (§10 PRD), dan yang dihapus adalah salinannya di milestone. Rujukan grup (`FR-X`) tidak dipakai di milestone karena ia diam-diam menyapu anggota P1/P2; yang ditulis adalah rentang eksplisit.

### M0 — Fondasi & Kontrak Modul · 5–7 pw

Gerbang risiko. Tidak ada fitur bisnis di sini; yang dibangun adalah hal-hal yang mahal diubah nanti.

**Isi:** struktur monorepo · `packages/db` dengan deskriptor netral + codegen per dialect (§4.3) · generator UUIDv7 tunggal, monotonik dalam milidetik (§4.3.1, O-6) · `packages/config` loader env tervalidasi (P-4) · `packages/module-kit` **sebagai paket nyata, bukan alias tsconfig** (§4.9 poin 1) · Elysia + SvelteKit tersambung lewat Eden Treaty · `modules:sync` + `modules.json` (G-2, G-10) · bentuk modul, impor lewat paket, isolasi kegagalan modul, validasi namespace (G-1, G-3, G-7, G-9) · **event bus + penjadwal core aman multi-instance (G-17, G-18)** · migrasi & seed (O-1…O-6) · **CI matriks tiga dialect: MySQL 8 + MariaDB 11 + PostgreSQL 16** (P-9) · harness test unit + integrasi dengan DB nyata via testcontainer (P-8 bagian pertama — rantai E2E penuh menyusul di M5) · kerangka Docker Compose dev + prod, termasuk restart otomatis, rotasi log, dan volume unggahan (Q-1…Q-7, Q-9) · **dokumen kontrak modul** untuk developer (G-13, versi pertama)

**Gate keluar:**
1. `bun dev` menyalakan web + API; halaman kosong ter-SSR
2. Migrasi yang sama jalan bersih di **MySQL 8, MariaDB 11, dan PostgreSQL 16**; suite test (sekecil apa pun) lulus di ketiganya di CI, tanpa test yang di-skip pada dialect mana pun
3. Ada test yang membuktikan UUIDv7 **monotonik dalam milidetik yang sama** — sifat yang nanti diandalkan paginasi cursor (N-5), jadi jauh lebih murah diuji sekarang
4. Satu modul dummy menyumbang tabel + route API + halaman + menu, **tanpa mengubah berkas core** — diperiksa `git diff`
5. **Satu modul dummy kedua dipasang dari repositori git terpisah** lewat `modules:add`, dan berfungsi sama
6. Sebuah job terjadwal yang didaftarkan modul dummy berjalan **tepat sekali** dengan `--scale api=3`, dan sebuah event core yang diterbitkan memanggil hook modul itu — keduanya dibuktikan test, bukan diasumsikan
7. `compose.prod.yml` menyajikan halaman di balik Caddy dengan TLS

> **Kenapa penjadwal masuk M0 (gate 6), bukan M7 bersama operasi.** Empat kebutuhan P0 sudah mengandaikannya sejak awal — pembersihan `sessions`, worker outbox (J-2), retensi log AI (M-3), dan backup terjadwal (Q-8) — dan sifat yang mahal itu bukan "menjalankan job", melainkan **menjalankannya sekali saat instance-nya tiga**. Penjadwal naif akan lolos di dev satu proses dan baru salah di produksi. Sama seperti gate 5: ini kontrak, dan kontrak tidak boleh ditemukan setelah ada pemakainya.

> **Gate 5 sering ditunda orang ke akhir proyek. Jangan.** Kalau `@core/*` telanjur jadi alias tsconfig, memperbaikinya di M6 berarti menyentuh setiap impor di setiap modul. Di M0, biayanya satu hari.

**Titik keputusan:** bila codegen lintas-dialect terbukti terlalu mahal di sini, turunkan cakupan **sekarang** — ke keluarga MySQL saja (MySQL + MariaDB, yang jauh lebih dekat satu sama lain daripada ke PostgreSQL), atau pertimbangkan Kysely (§10). Keputusan ini murah di M0 dan mahal di mana pun setelahnya.

---

### M1 — Identitas · 5–7 pw

**Isi:** auth (A-1…A-7, A-10, A-12) — Argon2id, sesi cookie `httpOnly`, CSRF menyeluruh, verifikasi email, reset password · tenancy (B-0…B-5) · RBAC (C-1…C-7) · CRUD user/group/client + profil (D-1…D-4) · adapter sesi & rate limit versi `database` (Keputusan M)

**Gate keluar:**
1. Login → kelola user → atur izin group → ganti tenant, seluruhnya berfungsi
2. Ada test yang membuktikan request lintas-origin ditolak pada endpoint yang mengubah state (§8 #10)
3. Ada test yang membuktikan **query tanpa filter tenant tidak bisa lolos** — penjaga B-3 bekerja di lapisan data, bukan di handler
4. `--scale api=3` **tanpa Redis**: login dan CRUD berfungsi di ketiga instance tanpa sticky session

> Gate 3 adalah konsekuensi langsung dari Keputusan N (tenancy per kolom). Tanpa batas fisik database, satu-satunya yang mencegah kebocoran lintas-tenant adalah kode — jadi itu harus diuji, bukan diasumsikan.

---

### M2 — Kerangka UI, Tema, & Layout · 6–8 pw

Bisa berjalan paralel dengan M1 setelah M0 selesai.

**Sudah selesai sebelum milestone dimulai:** token, manifest, registry ikon & layout, dan validator empat tema bawaan — lihat [`THEMES.md`](./THEMES.md). Yang tersisa adalah mengisi, bukan memutuskan.

**Isi:** komponen shadcn-svelte (L-1) · **DataTable** (L-16) dan **FormBuilder** (L-17) — dua komponen terbesar di proyek ini, sisihkan waktunya · komponen `<Icon>` + pemetaan tiga set ikon (L-5) · enam komponen layout terdaftar (L-6…L-8) · resolusi tema & varian layout saat SSR (L-11) · pemilih tema tanpa JavaScript (L-12) · i18n (K-1…K-7) · menu terfilter izin (F-1…F-4) · registry widget dashboard (G-19) · aksesibilitas (L-21) · font & aset merek

**Gate keluar:**
1. Empat tema bisa dipilih; berpindah tema mengubah warna, ikon, **dan** layout
2. Halaman pertama ter-render bertema benar **tanpa kedipan dan tanpa pergeseran tata letak**, termasuk untuk pengunjung anonim
3. Satu halaman mendeklarasikan `layoutVariant: 'wide'` dan tampil beda, sementara halaman lain tidak berubah
4. `node packages/ui-theme/validate.mjs` hijau di CI
5. Satu layout kustom didaftarkan dari luar core dan dipakai — **tanpa satu halaman pun diubah**
6. Dashboard berfungsi dengan JavaScript dimatikan: login, satu alur CRUD, pemilih tema, pemilih bahasa

---

### M3 — Konfigurasi, Kontrak API, & Halaman Baku · 3–4 pw

**Isi:** konfigurasi runtime + form ter-generate (E-1…E-8) · adapter cache versi `database` (E-5) · resolusi landing/home route (F-5…F-7, §4.7) · OpenAPI runtime + typed client (N-1…N-7) · observability dasar (M-1, M-4, M-5) · **penyamaran data sensitif di logger (M-7)** · audit log (M-2) · aktif/nonaktif modul per tenant (G-8) — gate M5 #3 bergantung padanya

**Gate keluar:**
1. Admin mengubah setelan, tema baku, dan landing page dari UI — berlaku **tanpa restart**, dan terlihat oleh ketiga instance
2. `/openapi.json` cocok dengan perilaku nyata; klien typed terkompilasi tanpa error
3. Route baku yang menunjuk modul nonaktif jatuh ke fallback aman, bukan 404 di `/`

---

### M4 — Modul `Example` & sisi publik · 3–4 pw

Bisa paralel dengan M5.

**Isi:** modul `Example` (R-1…R-8) · landing komersil dengan data dari DB (§4.6) · SEO per halaman + sitemap (R-4) · form kontak tanpa JavaScript (R-5) · email + outbox (J-1…J-3)

**Gate keluar:**
1. `/` menyajikan landing komersil ter-SSR pada instalasi bersih
2. Lighthouse **≥ 90 pada keempat kategori** di landing — Performance, Accessibility, Best Practices, **dan SEO** (persis kriteria §8 #8, jangan disempitkan); terindeks dengan JavaScript dimatikan
3. Form kontak bekerja tanpa JavaScript, tersimpan, dan emailnya masuk outbox
4. `Example` dinonaktifkan → aplikasi tetap utuh, `/` jatuh ke fallback

> Milestone ini adalah uji pertama titik perluasan 13 dan 14 (halaman publik & tema). Kalau kontraknya kurang, ketahuannya di sini.

---

### M5 — Modul `AI` · 3–4 pw

Bisa paralel dengan M4.

**Isi:** chat streaming end-to-end (H-1…H-9) · riwayat percakapan · render markdown tersanitasi · log AI asinkron · **dibangun sebagai modul**, bukan bagian core · **E2E penuh landing → login → CRUD → chat (P-8)** — baru bisa ditulis di sini karena chat-nya baru ada

**Gate keluar:**
1. Streaming berjalan provider → API → SvelteKit → UI, dengan pembatalan yang benar saat tab ditutup
2. Setiap panggilan tercatat dengan token & latensi terisi, dan pencatatan **tidak menahan** jalur panas
3. Menonaktifkan modul AI untuk satu tenant menghilangkan menu, route, dan tool-nya — sisa aplikasi utuh
4. Melepasnya dari `modules.json` membuat aplikasi tetap ter-build dan berjalan **tanpa jejak AI**

> Ini uji terberat kontrak modul: fitur besar yang menyentuh titik perluasan 1–12. Gate 4 yang paling sering gagal diam-diam — sisa impor atau entri menu yatim.

---

### M6 — Modul lintas repositori · 3–4 pw

Gerbang janji produk. Setelah ini, "developer bisa membangun modul sendiri" berhenti jadi klaim.

**Isi:** `bun modgen` lengkap (G-4) · starter modul standalone (G-12) · `modules:add` + penguncian `ref` (G-10) · pemeriksaan `engines.core` (G-11) · **penjaga CI "tanpa ubah core"** (G-6) · **16 titik perluasan tersedia seluruhnya, masing-masing dengan contoh yang jalan (G-5)** · dokumen "Membangun Modul Pertama Anda" (G-13, versi final)

**Gate keluar:**
1. Modul dibuat lewat `bun modgen` dan langsung berfungsi — tabel, menu, izin, CRUD, terjemahan
2. Modul dibuat di **repo terpisah**, di-build & dites di sana, dipasang lewat git URL, berfungsi penuh
3. CI **menolak** perubahan yang menyentuh berkas core saat menambah modul
4. Menghapus folder modul + `modules:sync` → aplikasi tetap benar, tanpa route yatim atau tema hilang
5. Seseorang di luar tim inti mengikuti dokumen G-13 dan berhasil sampai modulnya jalan — **tanpa bertanya**

> Gate 5 tidak bisa diotomasi dan sering dilewati. Ia satu-satunya yang benar-benar menguji apakah dokumennya cukup.

---

### M7 — Pengerasan & Operasi · 3–4 pw

**Isi:** **retensi log & audit (M-3)** — fasilitas audit log-nya sendiri sudah ada sejak M3, yang tersisa di sini kebijakan retensinya · backup/restore teruji (O-7, Q-8) · panduan deploy VPS (Q-10) · dokumentasi (P-10) · **pembuktian manual kriteria §8** — VPS bersih, restore, stabilitas 2 vCPU / 4 GB

Yang **tidak** ada di sini, dan sengaja: MCP, systemd, deploy tanpa downtime, preflight, resource limit, CD contoh, dan adapter Redis. Semuanya P1/P2 — rumahnya §8 (butir 2, 3, dan 7), dan ID-nya sengaja tidak ditulis ulang di sini supaya milestone ini tetap terbaca P0-saja oleh pemeriksaan otomatis. Estimasinya turun dari 4–6 pw menjadi 3–4 pw karena itu.

**Gate keluar — ini titik rilis MVP:** seluruh 25 kriteria terima §8 hijau, termasuk yang hanya bisa diuji manual:
1. Dari VPS bersih ke HTTPS dalam < 15 menit mengikuti panduan, **tanpa langkah tak tertulis**
2. Backup diambil → database dihapus → restore → aplikasi utuh. Dijalankan, bukan diasumsikan
3. Stack stabil di 2 vCPU / 4 GB dengan RAM idle < 1,5 GB
4. Suite yang sama lulus dengan **dan** tanpa Redis

---

## 4. Jalur kritis & paralelisasi

**Jalur kritis:** M0 → M1 → M3 → M6 → M7. Ini yang menentukan tanggal selesai.

| Fase | Developer A | Developer B |
|---|---|---|
| 1 | M0 — db codegen, deskriptor, migrasi | M0 — module-kit, `modules:sync`, event bus & penjadwal, CI, compose |
| 2 | M1 — auth, tenancy, RBAC | M2 — komponen, DataTable, FormBuilder |
| 3 | M1 — CRUD user/group/client | M2 — tema, layout, ikon, i18n |
| 4 | M3 — konfigurasi, OpenAPI, observability | M3 — form ter-generate, resolusi route |
| 5 | M5 — modul AI | M4 — modul Example, landing, SEO, email |
| 6 | M6 — modgen, starter, CI guard | M6 — contoh 16 titik perluasan (G-5), dokumen |
| 7 | M7 — retensi log, uji stabilitas & VPS bersih | M7 — backup/restore teruji, panduan deploy |

Dengan **1 developer**, kalikan kalender ±1,8× (bukan 2× — hilangnya biaya koordinasi menutup sebagian). Dengan **3 developer**, jalur kritis tidak banyak berubah; yang bertambah adalah kemampuan menyerap P1 lebih awal.

---

## 5. Gate keputusan

Pertanyaan terbuka §11.2, dipetakan ke kapan jawabannya benar-benar dibutuhkan.

| # | Pertanyaan | Dibutuhkan paling lambat | Bila belum dijawab |
|---|---|---|---|
| 1 | Gaya landing `Example` (company profile / e-commerce) | **M4, sebelum mulai** | Memblokir — bentuk tabel & halamannya berbeda |
| 2 | Aset merek per tema (logo, favicon, pratinjau) | M2 | Pakai placeholder; tidak memblokir |
| 3 | Pemilik registry harga model AI | M5 | Hardcode dulu di tabel konfigurasi; pindah belakangan murah |
| 4 | Lisensi rilis template | Sebelum rilis | **Diputuskan 2026-09-09, diubah 2026-09-10: MIT** — `LICENSE`, `"license": "MIT"` di semua workspace (PRD §11.1) |

Hanya #1 yang benar-benar mengikat. Sisanya bisa berjalan dengan nilai sementara.

**Sudah diputuskan dan tidak lagi memblokir M0:** image baku `compose.prod.yml` adalah **MySQL 8**; MariaDB 11 tetap tier-1 tapi lewat job CI-nya sendiri (§4.3, P-9).

---

## 6. Penjaga CI, dinyalakan bertahap

Kriteria terima §8 bukan pemeriksaan sekali di ujung. Kolom kanan adalah kapan pemeriksaannya mulai wajib hijau.

| Pemeriksaan otomatis | Kriteria §8 | Nyala di |
|---|---|---|
| Matriks test MySQL 8 + MariaDB 11 + PostgreSQL 16 | #2 | M0 |
| DDL menyatakan charset & collation eksplisit (tidak mewarisi default server) | §4.3 | M0 |
| `git diff` menolak perubahan core saat menambah modul | #11, #18 | M0 (dasar) → M6 (penuh) |
| Modul dari repo terpisah ter-build & jalan | #13 | M0 (dummy) → M6 (penuh) |
| Grep artefak build: tidak ada rahasia di bundle klien | #6 | M0 |
| Lint: warna literal & impor ikon langsung ditolak | L-2, L-5 | M2 |
| `packages/ui-theme/validate.mjs` — kontras AA semua tema | #17, #21 | M2 |
| Uji aksesibilitas per kombinasi layout × tema | #17 | M2 |
| Suite E2E tanpa JavaScript | #5 | M2 |
| `bun check` — klien typed vs OpenAPI | #4 | M3 |
| Test CSRF lintas-origin ditolak | #10 | M1 |
| Test kebocoran lintas-tenant | B-3 | M1 |
| Lighthouse landing ≥ 90 — Performance, Accessibility, Best Practices, SEO | #8 | M4 |
| `--scale api=3` **tanpa Redis** | #3 | M1 (dasar) → M7 (penuh) |
| Job terjadwal berjalan tepat sekali di bawah `--scale api=3` | D1, G-18 | M0 |
| Audit dependensi | #7 | M0 |

---

## 7. Definisi selesai (per perubahan, bukan per milestone)

Sebuah perubahan dianggap selesai bila:

1. Lint, format, dan type-check lulus (P-7)
2. Ada test pada tingkat yang sesuai — unit untuk logika murni, integrasi untuk yang menyentuh DB
3. Endpoint baru punya skema TypeBox, sehingga otomatis muncul benar di OpenAPI (N-1)
4. Tidak ada string yang di-hardcode di komponen (§7 i18n)
5. Tidak ada warna literal atau impor glyph langsung (L-2, L-5)
6. Query yang menyentuh tabel ber-tenant lewat helper repository, bukan query langsung (B-3)
7. Bila mengubah perilaku yang tertulis di PRD, **PRD ikut diperbarui pada PR yang sama** — dokumen yang basi lebih berbahaya daripada tidak ada dokumen

---

## 8. Sesudah MVP

Ini **satu-satunya rumah** untuk P1/P2 (invarian §3). Urutan yang disarankan, berdasarkan apa yang paling cepat terasa oleh pemakai template:

1. **UI admin modul + editor tema** (G-14, L-24) — **selesai 2026-09-08.** Halaman Modul menampilkan sumber, versi, path, deskripsi, kontribusi tiap modul (dihitung `modules:sync`), dan kesehatan (status job terakhir dari `scheduler_jobs`, hook yang gagal di instance ini); editor tema `/themes` merakit tema kustom dari token + set ikon + layout terdaftar, divalidasi kontras AA, tersimpan di tabel `themes` dan berlaku tanpa deploy ([`THEMES.md` §5a](./THEMES.md)). Unggah logo menyusul bersama Q-16 (butir 5)
2. **MCP server & client** (FR-I) — **selesai 2026-09-08** ([`MCP.md`](./MCP.md)): kontrak tool (titik perluasan 8), MCP server `/v1/mcp` dengan SDK resmi (I-1, I-2, I-3) di atas registry `apps/api/src/tools.ts`, token API bearer (A-4, sebelumnya belum ada), MCP client di modul AI (I-4: `ai_mcps`/`ai_mcp_tools`, transport http/sse, tool eksternal masuk registry lewat *tool source* dan ikut ditawarkan di chat AI; I-5: UI `/m/ai/mcps`), dan I-6 dibuktikan test (`apps/api/test/integration/mcp.test.ts`, `tools.test.ts`, `modules/AI/test/integration/mcp-client.test.ts`). **Keputusan (dikonfirmasi 2026-09-08):** transport MCP hanya HTTP / Streamable HTTP (klien juga menerima SSE lama) — `stdio` dan `websocket` **tidak akan dibangun**; endpoint MCP server adalah satu endpoint JSON-RPC Streamable HTTP, bukan `POST /tools/list` dkk. seperti di Lampiran A PRD
3. **Preflight + deploy tanpa downtime** (Q-13, Q-12) — **selesai 2026-09-08** bersama systemd (Q-11) dan batas sumber daya (Q-14): `api preflight` (subperintah binary, `dc run --rm preflight`, `ExecStartPre` di systemd), `deploy/upgrade.sh` + `deploy/rollout.sh` (replika baru berdampingan → sehat → Caddy retry → yang lama dimatikan; dibuktikan `scripts/ci/rollout-proof.sh` dengan nol request gagal), `mem_limit`/`cpus` per service, `deploy/systemd/*.service` + `scripts/build-release.sh` ([`DEPLOY.md` §8](./DEPLOY.md)). Yang tidak masuk: upgrade image mysql/caddy tanpa downtime, dan rollout tanpa downtime di mode systemd
4. **Notifikasi dalam aplikasi** (J-4) — **selesai 2026-09-08**: tabel `notifications` (migrasi 0010), layanan `notify()` (`@app/api/notifications`: penerima eksplisit atau *fan-out* ke semua pemegang sebuah izin di tenant, pelaku dikecualikan), event `notification.created`, API `/v1/notifications` (daftar, hitung, tandai dibaca, tandai semua; selalu milik pemanggil di tenant aktif), bel di header semua layout dasbor + halaman `/notifications` tanpa JS, retensi notifikasi terbaca (`logs.notification_retention_days`). Produsen bawaan: token API dibuat → pemilik; modul di-toggle → pemegang `module.manage`; pesan kontak Example → pemegang `example.inquiry.read`. **Dropdown bel menyusul 2026-09-10**: `<details>` di shell (`apps/web/src/routes/(app)/+layout.svelte`) berisi lima notifikasi belum dibaca terbaru — membukanya, menandai satu dibaca, dan **tandai semua** semuanya form biasa ke aksi `/notifications`, jadi tetap jalan tanpa JavaScript (`use:dropdown` hanya menambah tutup-saat-klik-luar/Escape). Satu panggilan API memberi makan lonceng dan isinya sekaligus (`?unread=1&limit=5` mengembalikan total belum dibaca bersama barisnya), jadi shell tidak jadi lebih mahal dari sekadar hitungan. Dibuktikan lima cek di `scripts/m1-gate1-proof.ts` §7. Push/webhook keluar (J-5) sudah mendarat di P2 butir 2
5. **Fitur unggah berkas + adapter S3** (Q-16) — **selesai 2026-09-08**: paket `@core/storage` (adapter `local` di volume Q-9 dan `s3` lewat klien S3 bawaan Bun — AWS/MinIO/R2/Spaces, dipilih `STORAGE_DRIVER`), tabel `files` (migrasi 0011; metadata = data tenant, kunci objek `<clientId>/<yyyy>/<mm>/<id>.<ext>`), layanan `storeUpload()` di `@app/api/files` untuk modul (validasi ukuran & tipe dari **Pengaturan → Berkas**, endus magic bytes, refusal `content_mismatch`), API `/v1/files` (izin `file.*`, privat = pemilik/`file.read`, publik = anonim dengan tenant, SVG disajikan dengan CSP sandbox), preflight memeriksa volume atau bucket, dan pelunasan L-24: editor tema mengunggah logo → `themes.assets` → header semua shell ([`MODULES.md` §3](./MODULES.md), [`DEPLOY.md` §3](./DEPLOY.md), [`THEMES.md` §5a](./THEMES.md)). Menyusul sesudahnya: **UI favicon** di editor tema (`ThemeEditor.svelte` + `_assets.server.ts`, berkas publik `theme-favicon`) dan **avatar pengguna** (Profil → unggah/hapus, `POST /v1/users/profile/avatar`, maks 2 MB). Belum: pemindahan objek antar driver (local ↔ S3) — tidak ada perintah migrasinya, ganti driver berarti memindahkan bucket sendiri
6. **Multi-provider AI + dashboard biaya** (H-10, H-15) — **selesai 2026-09-08**: profil penyedia per tenant (`ai_providers`) dengan daftar model berharga (`ai_models`, migrasi 0012), dipilih per percakapan dari header chat atau dinamai di request (`provider`, `model`); biaya tiap panggilan dari baris harga model yang menjawab, kode penyedia tercatat di `ai_calls.provider`; halaman **Penyedia AI** (CRUD, key tersamar, uji `GET /models`), izin `ai.provider.*`; Pengaturan → AI tetap jadi penyedia implisit bila belum ada profil. **Analitik AI** (`/m/ai/analytics`): total, token per hari (grafik CSS), per penyedia/model, per pengguna, rentang 7/30/90 hari ([`AI.md`](./AI.md)). Menyusul sesudahnya: kuota per tenant/user (H-14, P2 butir 3), lampiran & threading pesan (H-11, H-12, P2 butir 7), dan **widget dasbor penggunaan** — sejak widget boleh menyebut path `data` (G-19), `/dashboard` menarik `/v1/m/ai/analytics?days=30` sebagai pengguna itu dan menyerahkannya sebagai prop ke `modules/AI/web/widgets/Usage.svelte`
7. **Adapter Redis** untuk sesi, cache, dan rate limit — **selesai 2026-09-08**, opsional per jenis state lewat `SESSION_DRIVER` / `CACHE_DRIVER` / `RATELIMIT_DRIVER` = `redis` + `REDIS_URL`; database tetap sumber kebenaran: sesi di-cache 60 s di depan tabel `sessions` dan diusir oleh setiap jalur tulis (logout, revoke-all, ganti/lepas tenant), rate limit memakai `INCR`+`PEXPIREAT` per jendela, dan setiap galat Redis jatuh kembali ke database (fail-open ke database, bukan ke "tanpa batas"). CI gate M7 menjalankan seluruh suite integrasi dengan ketiga driver `redis` + `TABLE_PREFIX` ([`DEPLOY.md` §3](./DEPLOY.md), `packages/auth/src/redis.ts`)
8. **Metrik Prometheus** (M-6) — **selesai 2026-09-08**: paket `@core/metrics` tanpa dependensi (counter/gauge/histogram + format teks 0.0.4), `GET /metrics` di root api (tidak diproksi Caddy; `METRICS_TOKEN` opsional, `METRICS_ENABLED`), laju request/latensi/error per route yang cocok, in-flight, pool DB, job penjadwal, hook modul, `app_info`, proses; modul AI menyumbang `ai_calls_total`/`ai_tokens_total`/`ai_cost_micro_total`; modul lain lewat `@app/api/metrics`; profil compose `monitoring` (Prometheus + `deploy/prometheus.yml` dengan DNS SD ke semua replika) ([`DEPLOY.md` §7a](./DEPLOY.md)); Grafana ter-provision (datasource + dashboard API 12 panel, `deploy/grafana/`) dan 7 aturan alert (`deploy/prometheus-rules.yml`) di profil yang sama. Alertmanager ikut di profil yang sama (2026-09-08; kanal email/Slack/Telegram/webhook diisi operator di `deploy/alertmanager.yml`)

P2 (2FA, SSO tambahan, impersonasi, job queue, marketplace modul) sebaiknya menunggu pemakaian nyata. Menambahkannya lebih awal berarti menebak kebutuhan yang belum ada — persis risiko "cakupan membengkak" di §10 PRD.

**P2 yang dikerjakan atas keputusan pemilik (2026-09-08)**, urutan berdasar nilai per usaha:

1. **Pipeline CD contoh** (Q-15) — **selesai**: `.github/workflows/deploy.yml` (build + push ke GHCR → SSH → `deploy/upgrade.sh --pull <tag>`), mode `--pull` di upgrade.sh ([`DEPLOY.md` §8d](./DEPLOY.md))
2. **Webhook keluar** (J-5) — **selesai**: tabel `webhooks` + `webhook_deliveries`, event inti tenant → POST bertanda tangan HMAC-SHA256 dengan retry 1m/5m/30m/2h/12h, halaman `/webhooks` (secret sekali tampil, ping uji, riwayat, coba lagi), retensi riwayat, metrik ([`WEBHOOKS.md`](./WEBHOOKS.md))
3. **Kuota AI per tenant/user** (H-14, B-6) — **selesai**: kuota token bulanan tenant dan per pengguna (Pengaturan → AI), saldo prabayar per tenant (`ai_credits` + `ai_credit_ledger`, migrasi 0014) yang dikurangi biaya tiap panggilan; ditolak 429 dengan alasan sebelum penyedia dipanggil; kartu Kuota & saldo + top-up di Analitik AI, sisa kuota di header chat ([`AI.md`](./AI.md))
4. **2FA TOTP + recovery codes** (A-11) — **selesai**: TOTP RFC 6238 tanpa dependensi (`@core/auth` `totp.ts`, diuji dengan vektor RFC), tabel `user_mfa` + `mfa_challenges` (migrasi 0015); Profil → Autentikasi dua faktor: siapkan (QR SVG di server + kunci manual), aktifkan dengan kode hidup (10 kode pemulihan sekali tampil, sesi lain diakhiri), buat ulang kode, matikan dengan kata sandi; login dua langkah tanpa JavaScript (`POST /v1/auth/login` → `mfaRequired` + challenge 5 menit, `POST /v1/auth/login/mfa` dengan TOTP atau kode pemulihan, 5 kode salah membakar challenge, anti-replay per langkah); admin `DELETE /v1/users/:id/mfa` untuk pemulihan lock-out
5. **Impersonasi user** (D-6) — **selesai**: `POST /v1/users/:id/impersonate` (izin `user.impersonate`; bukan diri sendiri/superadmin lain/nonaktif; non-superadmin hanya anggota tenant aktifnya dan hanya target yang izinnya tidak melebihi miliknya, sesi impersonasi dipaku ke tenant aktif itu; 1 jam) membuka sesi atas nama target dengan kolom `sessions.impersonator_id` (migrasi 0016) dan mengirimnya lewat cookie kedua `crk_impersonate` — sesi admin sendiri tidak disentuh; permintaan berjalan sebagai target, `/v1/auth/me` menyebut `impersonator`; ubah kata sandi/2FA dan impersonasi bersarang ditolak 403 `impersonating`; penanda merah bernama pengguna yang diperankan di menu akun (`#account-menu`, kanan atas setiap halaman dasbor) dengan tombol Berhenti di dalamnya (`POST /v1/users/impersonate/stop` mencabut sesi + hapus cookie); tombol "Masuk sebagai …" di halaman detail pengguna dan tombol ikon di daftar pengguna (`/users`, di kanan tombol Ubah); audit `user.impersonate_start` / `user.impersonate_stop`; logout mengakhiri kedua sesi
6. **Floating chat button dengan konteks halaman** (H-13) — **selesai**: titik perluasan 11 mendapat `slot: 'shell'` (widget dirender di setiap halaman dasbor setelah layout, filter izin + modul aktif di server, komponen code-split); modul AI menyumbang `ai.floating_chat` (`web/widgets/FloatingChat.svelte`): tombol pojok kanan bawah (tanpa JS = tautan ke `/m/ai/chat`), panel streaming lewat jembatan yang sama, konteks halaman (breadcrumb, path, judul, teks tersorot) dikirim sebagai field `context` → pesan `system` kedua di API, tidak pernah disimpan ke riwayat; E2E Playwright menguji panel dari `/dashboard` ([`AI.md`](./AI.md), [`MODULES.md`](./MODULES.md))
7. **Lampiran pesan AI (H-11) dan threading (H-12)** — **selesai**: `POST /v1/m/ai/attachments` (berkas privat milik pengirim lewat Q-16, gambar/teks/CSV/JSON/MD ≤ 5 MB) + `attachments` di `chat/completions` (teks dikutip, gambar sebagai `image_url`), tabel `ai_attachments`; `ai_messages.parent_id` (migrasi 0017) dengan `parent_id`/`regenerate` di API, `x_messages`/`crk.messages` mengembalikan id tersimpan, backfill percakapan lama saat dibaca; halaman chat: klip lampiran, tombol Buat ulang / Ubah-lalu-cabang, navigasi versi ‹ 1/2 › (`?m=<id>`), helper murni `web/lib/thread.ts` ([`AI.md`](./AI.md))
8. **Dukungan RTL** (K-9) — **selesai**: arah tulisan diputuskan di server (`directionOf(locale)` di `@core/i18n`, bahasa Arab/Ibrani/Persia/Urdu dll. → `dir="rtl"` di `<html>` sebelum byte pertama, sejalan K-2); shell, layout bawaan, komponen UI, dan halaman modul memakai utilitas CSS logis Tailwind (`ms-/me-/ps-/pe-/start-/end-/text-start/text-end/border-s/e`) sehingga satu stylesheet melayani dua arah, ikon penunjuk arah berputar lewat `rtl:rotate-180`; **Pratinjau RTL** di halaman Bahasa (cookie `crk_dir`) memaksa `rtl` tanpa memasang terjemahan; gate M2 memeriksa `dir` dan ketiadaan kelas fisik di shell
9. **Registry/marketplace modul sederhana** (G-16) — **selesai**: katalog JSON `modules.catalog.json` (atau `MODULES_CATALOG_URL`/`MODULES_CATALOG_FILE`; skema + `compareCatalog`/`searchCatalog` di `@core/module-kit`), CLI `bun modules:search [teks]` dan `bun modules:install <Nama>` (menyelesaikan repo + ref terkunci → `modules:add`), `GET /v1/module/catalog` (izin `module.manage`) dan bagian *Katalog modul* di halaman Modul dengan status terpasang/pembaruan/tersedia/tidak cocok (G-11) serta perintah pasang apa adanya — memasang tetap tindakan waktu-build, bukan tombol runtime ([`MODULES.md` §7c](./MODULES.md))
10. **Queue pekerjaan ad-hoc** — **selesai**: tabel `queue_jobs` (migrasi 0018), `registerTask()`/`enqueue()` dari `@app/api/queue` (payload, prioritas, `runAt`/`delayMs`, `dedupeKey`, `maxAttempts`, `timeoutMs`), worker `core.queue.work` tiap 10 dtk + nudge dengan klaim UPDATE bersyarat (sekali per baris di multi-instance), backoff 10 dtk/1/5/30 mnt/2 jam lalu **dead-letter**, pemulihan lease kedaluwarsa, izin `queue.read|manage`, halaman `/queue` (filter status, Ulangi/hapus teraudit, daftar task), retensi `logs.queue_retention_days`, metrik `queue_jobs_total`/`queue_job_duration_seconds` ([`MODULES.md`](./MODULES.md) → Antrean pekerjaan dari modul)
11. **Masuk dengan Google** (A-8, P1; diminta pemilik 2026-09-09) — **selesai**: authorization code + PKCE sepenuhnya di server (`GET /v1/auth/google/start` → cookie `crk_oauth` di web → Google → `/auth/google/callback` → `POST /v1/auth/google-login`; client secret tidak pernah meninggalkan API), tabel `oauth_accounts` (migrasi 0019; tautan lewat `sub`, bukan email), Pengaturan → Keamanan: aktif/nonaktif, client id, secret, **buat akun otomatis** (flag A-8), **domain email yang diizinkan**; bootstrap `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` di `.env` (E-6); email terverifikasi yang sudah terdaftar ditautkan, akun Google otomatis memverifikasi email lokal (A-6), pengguna nonaktif/domain asing/email belum terverifikasi ditolak dengan kode `sso_*`, rate limit terpisah `oauth:ip:`, 2FA tetap berlaku (challenge yang sama dengan login kata sandi), audit `auth.login` `{sso:'google'}` / `auth.sso_linked` / `auth.sso_account_created`; tombol di halaman masuk hanya tautan (tanpa JS). SSO lain (GitHub, X, Microsoft) tetap P2
12. **Uninstall modul bersih** (G-15, P1; diminta pemilik 2026-09-09) — **selesai**: `bun modules:remove <Nama> [--yes|--keep-files|--dry-run]` melepas `modules.json`, submodule (+`.gitmodules`, `.git/modules`, pengecualian `biome.json`) atau folder lokal, `bun install` + `bootstrap`, lalu **migrasi turun**: bila tabelnya sudah dirilis, migrasi hasil `db:generate` ditulis ulang — `DROP TABLE IF EXISTS` anak→induk dari snapshot drizzle (aman-FK di MySQL; drizzle-kit menulisnya alfabetis) + pembersihan baris core (`modules`, `configurations` `<ns>.*` dan route/tema baku yang menunjuk modul, `group_permissions`, `scheduler_*`, `queue_jobs`, `notifications`, `users.theme`, bump `cache_versions`); bila belum dirilis (migrasi masih untracked) berkas migrasi dikembalikan ke HEAD. Perencana murni di `@core/module-kit` (`remove.ts`, unit test), tidak menyentuh database (terapkan lewat `db:migrate` setelah backup dan setelah image baru ter-deploy). `ci:modgen-guard` dan `ci:cross-repo` kini mencabut lewat perintah ini; halaman Modul menampilkan perintahnya (`module.manage`). Yang dibiarkan: `audit_log`, baris/objek `files`, `app.allowed_themes`, tema kustom ([`MODULES.md`](./MODULES.md) §5)
13. **Registrasi lewat tautan undangan** (A-13, P1; diminta pemilik 2026-09-09) — **selesai**: tabel `invitations` (migrasi 0020; global dengan `client_id` eksplisit karena kode dicari tanpa tenant), `POST/GET/DELETE /v1/invitations` (izin `user.create`, tenant aktif), `GET /v1/auth/join/:code` + `POST /v1/auth/join` (mengabaikan `SIGNUP_ENABLED`, email dianggap terverifikasi, bergabung ke tenant pengundang, sesi langsung dibuka, rate limit `join:ip:`), `security.invitation_hours` (baku 72), email `invite` (+masa berlaku) dan `invite-existing` (alamat sudah terdaftar → ditambahkan ke tenant + info masuk), halaman `/join/<kode>` → `/auth/join/<kode>` tanpa JS, bagian **Undang lewat email** di halaman Pengguna (tautan ditampilkan sekali, daftar menunggu, cabut). Tes integrasi `invitations.test.ts`
14. **Pemantauan outbox email** (J-2, P1; diminta pemilik 2026-09-11) — **selesai**: halaman `/outbox` di grup **Pemantauan** (izin `mail.read`, aksi `mail.manage`) menampilkan seluruh isi tabel `outbox_email` **terbaru di atas** — keping status dengan hitungan dari seluruh tabel, filter status/template/rentang tanggal (`?from`/`?to` per hari kalender, `to` mencakup harinya penuh), pencarian bebas atas alamat penerima, nama penerima, dan subjek, kolom yang bisa diurutkan, ukuran halaman, "Kirim sekarang" (satu putaran worker) dan "Ulangi" per baris gagal. Dua lapis (L-20 di atas L-22): **dasarnya** tautan dan form GET biasa sehingga seluruh kontrol bekerja tanpa JavaScript, **peningkatannya** membuat kontrol yang sama menjawab lewat fetch — bilah filter diterapkan sambil mengetik (debounce 300 ms, `goto` dengan `keepFocus`/`noScroll`, satu entri riwayat per jeda ketik), keping/urutan/paginasi jadi navigasi sisi klien, sedangkan **Ulangi** dan **Kirim sekarang** memakai `use:enhance`: hasilnya jadi toast lalu `invalidateAll()` membaca ulang baris di tempat, tanpa URL berubah jadi `?saved=`/`?delivered=` (jalur tanpa JS tetap memakai notice itu). Filter selalu dikerjakan API, bukan browser. `GET /v1/outbox` kini menerima `?status`, `?template`, `?from`/`?to`, `?sort`/`?order` dan memakai `?q` untuk **pencarian** (sebelumnya `?q` berarti filter status, tanpa pemakai); `GET /v1/outbox/stats` ikut mengembalikan `total` dan daftar template yang dipakai. Dibuktikan `apps/api/test/integration/outbox.test.ts` (API) dan `apps/web/e2e/outbox.e2e.ts` (lapis fetch di peramban sungguhan: penanda di `window` bertahan, jadi tidak ada muat ulang halaman)
15. **Region `aside` bisa dipakai halaman + contohnya** (§4.8, L-19; diminta pemilik 2026-09-11) — **selesai**: keempat layout dasbor sudah punya region `aside` sejak M2 tapi tidak ada satu pun jalan bagi halaman untuk mengisinya, jadi region itu mati. Sekarang halaman **menyebut namanya** (`export const _aside = '<id>'` di `+page.server.ts`/`+page.ts`) — shell dirender lebih dulu sehingga halaman tidak bisa menyerahkan snippet. `bun run layout:variants` mengumpulkannya jadi `pageAsides` (route id → aside id), `(app)/+layout.server.ts` menyelesaikannya, `(app)/+layout.ts` meng-code-split komponennya dari `apps/web/src/lib/asides/registry.ts`, dan `(app)/+layout.svelte` meneruskannya **hanya** bila ada — jadi layout tetap satu kolom untuk halaman yang tidak memintanya. Komponen aside tidak menerima props; ia membaca data `load` halamannya lewat `page.data`, sehingga satu halaman memiliki konten utama dan kolom sampingnya sekaligus. Karena diselesaikan di server, kolom sampingnya ada di HTML pertama dan bekerja tanpa JavaScript. Contohnya halaman baru **`/examples/aside`** (L-19). Dijaga proof M2 #5 dua arah: halaman yang tidak menyebut aside tidak dapat kolom kedua, yang menyebut dapat.
16. **Sidebar bisa diciutkan, tersimpan per pengguna** (F-8, P1; diminta pemilik 2026-09-11) — **selesai**: keadaannya diputuskan di server seperti tema dan bahasa — profil pengguna (kolom `users.sidebar_collapsed`, migrasi 0023) → cookie `crk_sidebar` → terbentang — lalu ditulis sebagai `data-sidebar="expanded|collapsed"` di `<html>` sebelum byte pertama (`apps/web/src/lib/server/sidebar.ts` + `hooks.server.ts`), jadi rail sempit sudah benar di HTML pertama tanpa kedipan. Tombolnya `<SidebarToggle>` di `@core/ui`: **dasarnya** form POST ke `/sidebar` yang menulis cookie + profil lalu kembali ke halaman semula (jalan tanpa JavaScript, L-22), **peningkatannya** `use:enhance` yang membalik atribut seketika dan mengirim POST yang sama di latar — tanpa muat ulang. Layout-lah yang memiliki rail-nya: `sidebar-classic` menggambar tombol itu di kepala rail dan menata sendiri keadaan ciutnya (rail 3,5rem, label di-*clip* sr-only sehingga nama tautan tetap ada untuk pembaca layar dan axe), sedangkan `topnav-compact`/`centered-narrow` tidak menggambarnya sama sekali. Karena pilihannya milik pengguna (D-4), browser kedua yang belum pernah menyentuh tombolnya tetap membuka dasbor dengan rail yang sama — dibuktikan delapan cek F-8 di `scripts/m2-gate-proof.ts`, unit test `apps/web/src/lib/server/sidebar.test.ts`, dan profil di `apps/api/test/integration/admin.test.ts` ([`THEMES.md` §4](./THEMES.md)). **Dilengkapi 2026-09-11 setelah dicoba pemilik:** region nav menerima `rail: true` sehingga shell tahu bedanya rail dan menu vertikal biasa — setiap butir rail membawa **tooltip** namanya (label-nya sedang di-clip), dan tidak ada grup yang dirender terbuka saat ciut; **mengetuk ikon grup kini melebarkan sidebar** (aturan ciut mati saat `#sidebar-rail:has(details[open])`, jadi jalan tanpa JavaScript) dan `use:railExpand` membuat pilihan itu bertahan lewat tombol toggle yang sama. `dummy.two-column` (tema `dummy.ocean`) ikut mendapat rail yang bisa diciutkan — bukti bahwa layout **modul** melakukannya dengan dua langkah yang sama tanpa menyentuh core
17. **Generator tema** (P-11; diminta pemilik 2026-09-11) — **selesai**: `bun themegen <id> [--from base] [--module Nama] [--name …] [--icons …] [--layout-dashboard|public|auth …] [--dry-run|--force|--no-register]` menurunkan tema baru dari tema yang sudah lolos kontrak — salinan palet dengan **seluruh** selektor `[data-app-theme="…"]` ditulis ulang (terang dan gelap) dan manifest baru tanpa `assets`/`preview` yang mengarang berkas — lalu **mendaftarkannya**, bagian yang tidak bisa dilakukan `cp -r`: import manifest di `packages/ui-theme/src/registry.ts` (disisipkan urut path agar `lint` tetap hijau) dan import tokens di `apps/web/src/app.css`; tanpa keduanya tema tidak pernah muncul di runtime dan tokennya tidak ikut ter-bundle. `--module <Nama>` menulis ke `modules/<Nama>/themes/<id>` sebagai `<ns>.<id>` dan menjalankan `modules:sync` — nol berkas core berubah (titik perluasan 14). Set ikon/layout yang tidak terdaftar ditolak sebelum ada berkas ditulis (L-5, L-8), dan langkah terakhirnya `theme:validate` sehingga hasilnya lolos gate yang sama dengan tema tulis tangan (L-21). Logika murninya di `scripts/themegen/plan.ts` dengan 15 unit test (`plan.test.ts`) ([`THEMES.md` §5](./THEMES.md)). **Keputusan (2026-09-14): P-11 ditutup — generator seed tidak akan dibangun.** Ketiga generator yang dimintanya sudah terjawab: tema lewat `bun themegen`, migrasi lewat `bun db:generate` sejak M0, dan seed **sengaja tidak digenerate**. Bagian seed yang mahal adalah kerangkanya — `defineSeed`, idempotensi O-3, `client_id` per tenant, dan penjalinan ke `generated/seeds.ts` — dan itu sudah ditulis `bun modgen` untuk setiap modul baru (`scripts/modgen/templates.ts`, stub satu baris contoh). Yang tersisa hanyalah isi datanya: nama, harga, teks — justru bagian yang tidak boleh digenerate, karena seed adalah **demo yang meyakinkan** (R-3), bukan pengisi baris. Kebutuhan data bervolume juga bukan kebutuhan seed: tes yang perlu ratusan baris membuatnya sendiri di tempat, dan menumpuk baris palsu lewat `bun db:seed` merusak instalasi bersih — baris sisa di tenant baku yang melewati cap 100 baris per halaman sudah pernah membuat gate merah secara lokal. Bila nanti benar-benar dibutuhkan, bentuknya perintah dev sekali pakai (`bun dev:fill <Modul> --rows N`) yang **tidak** ikut `db:seed`, ditulis saat ada yang memintanya
18. **Varian landing kedua di modul Example** (R-9, P1; diminta pemilik 2026-09-11) — **selesai**: `/catalog` — susunan publik **kedua** dari modul yang sama di atas tabel yang sama (titik perluasan 13), gaya katalog bukan gaya toko: masthead tipis (tanpa hero/mosaik/paket harga), bilah filter lengket, daftar padat satu baris per produk, JSON-LD `ItemList` (bukan `Organization`), dan form kontak yang dipangkas jadi satu strip (`source: '/catalog'`). Pencarian/urutan dikerjakan **API** (`?q` atas nama+ringkasan, `?sort=name|price`, `?order`, `?featured` di `GET /v1/m/example/products`) supaya jawaban tanpa JavaScript sama persis: **dasarnya** form GET + tautan urut biasa, **peningkatannya** ketikan menerapkan filter setelah jeda 300 ms lewat `goto` (`keepFocus`/`noScroll`, satu entri riwayat per jeda) — pola yang sama dengan `/outbox`. Karena keduanya route publik biasa, `app.landing_route` bisa diarahkan ke salah satunya dan `/` berganti bentuk tanpa deploy (F-5, F-7) — itulah inti R-9. Dibuktikan tujuh cek R-9 di `scripts/m4-gate-proof.ts` (termasuk `/` yang dialihkan ke katalog lalu dikembalikan) dan `apps/api/test/integration/example.test.ts`
19. **Tombol kembali ke atas** (F-10; diminta pemilik 2026-09-12) — **selesai**: `<ScrollTop>` dirender sekali di root layout (`apps/web/src/routes/+layout.svelte`), jadi berlaku di **semua** jenis halaman — dasbor, auth, dan publik — tanpa layout mana pun ikut berubah. Tombolnya muncul setelah pembaca menggulir lebih dari 400 px (pendengar `scroll`/`resize` pasif) dan menggulir balik ke puncak dengan `behavior: "smooth"`, kecuali `prefers-reduced-motion: reduce` — maka melompat, sesuai permintaan pembaca. Ini satu-satunya kontrol shell yang **hanya ada bila JavaScript hidup**: tidak ada cara dokumen tahu sejauh apa halaman sudah digulir tanpanya, dan tombol yang selalu terpampang justru jadi derau — L-22 berbicara tentang form yang membawa pekerjaan, bukan tentang jalan pintas ke header yang tetap bisa dicapai. Pojok kanan bawah sudah ditempati widget shell (H-13), jadi ada kesepakatan kecil yang **seluruhnya CSS**: widget mengambang menandai dirinya `data-shell-dock="bottom-end"` (modul AI: `FloatingChat.svelte`) sehingga tombol ini bertumpuk di atasnya, dan `data-shell-dock-open` membuatnya menyingkir selama panel widget terbuka — `:has()` di `<body>`, tidak ada sisi yang perlu tahu sisi lain ada ([`MODULES.md`](./MODULES.md) → widget shell). Labelnya lewat i18n (`shell.scroll_top`, namespace anonim sehingga halaman publik ikut membawanya) dan ikonnya nama semantik `chevron-up` (L-5). Dibuktikan `apps/web/e2e/scroll-top.e2e.ts` di peramban sungguhan: tidak ada tombol di puncak halaman, ada setelah digulir, duduk di atas tombol chat, dan setelah diklik `window.scrollY` kembali 0 dan tombolnya hilang lagi
20. **Satu sesi per pengguna** (A-9, P1; mendarat 2026-09-12) — **selesai**, dicatat di sini menyusul karena §8 adalah satu-satunya rumah kemajuan P1/P2 (§3): flag `.env` `SINGLE_LOGIN_ENABLE` (baku `false`) membuat login yang berhasil **mencabut seluruh sesi lain** milik akun itu di server (`revokeAllSessions` di `@core/auth`, kecuali sesi yang baru dibuat), jadi peramban sebelumnya keluar pada permintaan berikutnya — bukan sekadar kehilangan cookie (A-5). Berlaku di semua pintu masuk yang memanggil `completeLogin`: kata sandi, langkah kedua 2FA, Google (A-8), dan penerimaan undangan (A-13); jumlah sesi yang dicabut ikut di audit `auth.login` sebagai `singleLoginRevoked`. Baku mati karena masuk di ponsel tidak boleh mengakhiri sesi di laptop kecuali deployment memintanya ([`README.md`](../README.md))
21. **Indikator online** (D-5, P1; diminta pemilik 2026-09-12) — **selesai**: kolom `sessions.last_seen_at` sudah lama ditulis (disentuh paling sering sekali per menit, `TOUCH_INTERVAL_MS` di `@core/auth`) tapi tidak pernah terlihat. Sekarang `GET /v1/users` dan `/v1/users/:id` mengembalikan `online` + `lastSeenAt`: **satu query per halaman** (`presenceOf()` mengambil seluruh baris sesi milik halaman itu lalu mereduksinya di memori, bukan satu query per baris), `online` hanya dari sesi yang masih hidup dan tersentuh dalam **5 menit**, sedangkan `lastSeenAt` adalah sentuhan terbaru dari sesi mana pun — termasuk yang sudah dicabut, supaya "terakhir aktif" tetap ada setelah seseorang keluar. API yang memutuskan ambangnya; web hanya menggambar. Di UI: titik di sebelah nama pada daftar pengguna (kolom **Aktivitas** yang bisa dinyalakan lewat menu Kolom mengejanya) dan di header halaman detail. Titik tidak pernah jadi satu-satunya penanda — teksnya selalu ada di DOM (terlihat, atau sr-only) dan di `title`. Kata-katanya dibangun di `$lib/presence` oleh halaman dasbor, bukan di dalam komponen, karena `lib/components` terjangkau pengunjung anonim dan seluruh kunci di sana wajib ikut payload i18n landing (§9) — presence adalah urusan dasbor. Dibuktikan `apps/api/test/integration/admin.test.ts` (masuk → online, akun baru → belum pernah, keluar → padam tapi `lastSeenAt` tetap) dan dua cek di `scripts/m1-gate1-proof.ts`
22. **Halaman panduan izin** (C-8, P1; diminta pemilik 2026-09-12) — **selesai**: `/permissions` (izin `group.read`, menu **Dokumentasi → Panduan izin**; lihat butir 26) menjelaskan apa yang selama ini hanya bisa disimpulkan dari editor grup — bentuk `sumberdaya.aksi` (aksi = segmen terakhir, sisanya sumber daya, jadi sumber daya modul boleh bertitik), tabel pencocokan yang menguji `user.edit`, `user.manage`, `user.*`, `*.edit`, `*.*` dan `user.read` terhadap **satu** kebutuhan yang sama, lalu dari mana izin seseorang datang (gabungan grup, per tenant aktif, superadmin melewati semuanya di C-5, kebutuhan selalu konkret, string rusak tidak pernah cocok). Di bawahnya **registry hidup** instalasi ini (`GET /v1/auth/permission-registry`, C-4) sehingga sumber daya modul muncul begitu modulnya terpasang, dan sebuah **pemeriksa**: form GET biasa yang jawabannya dihitung di server, jadi jalan tanpa JavaScript dan URL-nya bisa dibagikan sebagai penjelasan. Sambil membangunnya ketahuan `$lib/permissions` (cermin aturan API untuk UI, C-6b) **tidak** menolak kebutuhan ber-wildcard seperti yang dilakukan `@core/auth`; cermin itu diperbaiki dan sekarang dipaku ke aslinya oleh `apps/web/src/lib/permissions.test.ts` (seluruh pasangan grant × kebutuhan harus sepakat). Dibuktikan dua cek di `scripts/m1-gate1-proof.ts`
23. **Fitur MCP ikut bahasa pengguna** (K-4; diminta pemilik 2026-09-12) — **selesai**: halaman Server MCP (`/m/ai/mcps`, `/new`, `/[id]`) setengah diterjemahkan — judul, tombol, dan badge sudah lewat `t()`, tetapi seluruh formulirnya (label, hint, opsi transport, placeholder) tertulis mati bahasa Indonesia di `_form.ts`, begitu pula header tabel dan pesan kesalahan loader; pengguna ber-locale `en` mendapat halaman campur dua bahasa. Sebabnya bentuk deklarasinya: `mcpFields` sebuah **konstan**, dievaluasi saat modul dimuat, jauh sebelum ada locale — sekarang fungsi yang menerima `Translate`, pola yang sudah dipakai `webhookFields(events, t)`. Label yang dipakai bersama tabel dan form duduk di `ai.mcps.*`, yang hanya milik formulir di `ai.mcps.form.*` (karena `ai.mcps.enabled` sudah terpakai badge "aktif"). Sisi server memakai `createTranslator(event.locals.locale.locale)` untuk pesan `error()` dan dua kunci bersama `common.form_expired`/`common.check_fields`; tanggal uji terakhir mengikuti `useLocale()`, bukan `id-ID` mati. Badge `ok`/`error` sengaja dibiarkan mentah — itu nilai `last_status` apa adanya, bukan kalimat. Pesan **API** tetap bahasa Indonesia sesuai kebijakan yang ada
24. **Aksi baris seragam jadi ikon + umpan balik tombol** (L-21; diminta pemilik 2026-09-12) — **selesai**: `/users` sudah lama menggambar aksi barisnya sebagai ikon lewat `rowActions` DataTable, tetapi enam daftar lain masih mengejanya dengan teks. Yang memakai DataTable hanya kurang satu bendera yang sudah ada (`iconOnly` di `/groups`, `/tenants`, dua aksi `/examples/list`); yang menyusun `<Table>` sendiri harus menggambar tombolnya sendiri, dan di situ konstanta lokal `iconAction` mulai disalin — kelas itu kini `iconActionClass` yang diekspor `$lib/components/table`, dipakai DataTable sekaligus `/webhooks`, `/m/example/products`, `/m/ai/mcps`, dan `/m/ai/providers`. Di semua kasus teksnya pindah ke `title` **dan** `aria-label`: tooltip native (yang diposisikan sendiri akan terpotong kotak gulir mendatar tabel) dan nama untuk pembaca layar, keduanya tetap ikut locale. Dua keputusan yang bukan penerjemahan lurus: tautan "↗" ke halaman publik di `/m/example/products` ikut jadi tombol ikon (satu sel tidak boleh setengah ikon setengah simbol), dan tombol keluarkan-anggota di `/groups/[id]` memakai ikon `x` yang tetap netral — mengeluarkan seseorang dari grup bukan menghapus akunnya. Menyusul: tombol **Ulangi** di `/outbox` tinggal ikonnya (hanya ada satu aksi per baris, jadi teksnya tidak membedakan apa pun) dan ikon `refresh` pada uji koneksi/uji model AI ikut `animate-spin` selama probe berjalan — ikon diam di sebelah tulisan "Berjalan…" terbaca seperti halaman macet
25. **Uji koneksi per model penyedia AI** (AI-Roadmap F5; diminta pemilik 2026-09-12) — **selesai**: sampai F4 matriks kemampuan melekat pada **profil** penyedia, padahal yang diprobe selalu satu model (`default_model`) — di balik satu base URL lazimnya ada model reasoning, model chat saja, dan model tanpa function calling, jadi satu jawaban tingkat penyedia salah untuk sebagian besar baris daftar harganya. `POST /v1/m/ai/providers/:id/models/test` (izin `ai.provider.manage`) menjalankan probe §4.1 yang **sama persis** — `probeCapabilities()` sejak F0 sudah menerima id model sebagai argumen — dan menyimpan hasilnya di baris modelnya sendiri (migrasi **0024**, aditif di mysql+pg: tujuh kolom di `ai_models` yang mencerminkan milik `ai_providers`, sehingga `capabilitiesOf()` dan aturan "probe gagal tidak menghapus matriks yang sudah terbukti" berlaku tanpa kode baru). Modelnya diambil dari daftar penyedia, tidak pernah dari request sebagai teks bebas — probe membelanjakan key milik tenant, jadi model di luar daftar dijawab 404. `replaceModels` membawa matriks menyeberang penyimpanan (daftar harga ditulis ulang seluruhnya; matriks melekat pada **id model**, bukan pada baris yang memuatnya), dan uji tingkat penyedia ikut mengisi baris model bakunya. Di UI: kolom **Kemampuan** dan tombol **Uji model** per baris, hasil disimpan per id model (alasan menguji model kedua adalah membandingkannya dengan yang pertama), jalur `?/testModel` tetap jalan tanpa JavaScript. Dibuktikan satu penyedia dengan dua model berbeda verdict di balik satu base URL dan satu key ([`AI.md`](./AI.md), [`AI-Roadmap.md` §6.4](./AI-Roadmap.md))
26. **Grup menu Dokumentasi** (F-3; diminta pemilik 2026-09-12) — **selesai**: panduan izin (butir 22) duduk di grup **Pengaturan**, di antara halaman-halaman yang *mengubah* sesuatu, padahal ia bahan rujukan; sementara dokumentasi API yang dihasilkan API sendiri (`/docs`, Scalar di atas OpenAPI yang dibangun dari skema rute) tidak pernah ditautkan dari dasbor — satu-satunya cara menemukannya adalah mengetahui alamatnya. Keduanya kini grup **Dokumentasi**, ditaruh paling bawah — di bawah Pemantauan (urutan 910), karena bahan rujukan bukan tempat kerja harian — masing-masing membawa izinnya sendiri — `group.read` untuk panduan izin, `config.read` untuk dokumentasi API — sehingga pembaca yang tidak memegang keduanya tidak melihat grupnya sama sekali. Yang perlu ditangani: `/docs` hanya **tampak** seperti rute dasbor; ia dilayani API pada origin yang sama (Caddy di produksi, proxy Vite di pengembangan), jadi router SvelteKit yang mencoba merutekannya akan menjawab 404 — entri menu kini bisa menandai dirinya `external`, dan shell membukanya **di tab baru** (`target="_blank"` + `rel="noopener noreferrer"`), yang sekaligus menjawab keduanya: rujukan dibaca *di samping* dasbor, bukan menggantikannya, dan router tidak pernah menyentuhnya. Dilengkapi: tautan ke panduan izin dari bawah halaman `/groups` (halaman grup menampilkan izin sebagai kisi kotak centang — ia menjawab izin *mana*, tidak pernah apa *arti* satu izin), dan sakelar cakupan di `/settings` yang aktif kini terisi warna primer + `aria-current` alih-alih abu-abu samar yang membuatnya terbaca seperti dua tab setara — padahal cakupan yang terbuka menentukan apa yang **ditulis** saat menyimpan. Dibuktikan `apps/web/src/lib/server/menu.test.ts`
27. **Konfirmasi sebelum mengeluarkan anggota grup** (L-22; diminta pemilik 2026-09-12) — **selesai**: tombol keluarkan di daftar anggota `/groups/[id]` adalah satu POST tanpa tanya, padahal salah klik mencabut seluruh izin yang diberikan grup itu seketika dan mengembalikannya bukan "undo" melainkan mencari lagi orangnya di pemilih kandidat (yang sendiri harus dicari karena tenant bisa berisi ribuan pengguna). Pola barisnya persis `/queue`: pemicunya menaut ke halaman yang sama dengan barisnya disebutkan, `ConfirmDelete` mengubahnya jadi modal bila ada JavaScript, dan tanpa JavaScript server menggambar konfirmasinya di tempat. `&member=` membedakannya dari `?confirm=delete` milik grup itu sendiri — keduanya hidup di halaman yang sama — dan aksi `removeMember` kini menuntut `confirmed()` di server, karena konfirmasi yang hanya ada di markup bukan konfirmasi. `ConfirmDelete` menerima prop `icon` (baku tetap `trash`) sehingga barisnya tetap `x`: ikon tong sampah untuk "keluarkan dari grup" adalah peringatan yang salah. Kata-katanya menyebut akibatnya, bukan tindakannya
28. **Masuk lewat AJAX** (L-20 di atas L-22; diminta pemilik 2026-09-12) — **selesai**: halaman masuk mengirim **kedua langkahnya** (kata sandi, lalu kode 2FA) lewat `fetch` ke **API yang sudah ada** — `POST /v1/auth/login` dan `/v1/auth/login/mfa` — bukan lagi POST form biasa ke action halaman. `/v1` satu origin di setiap deployment (Caddy, contoh nginx/Apache, proxy Vite saat pengembangan — Keputusan E), jadi API **menaruh cookie sesinya langsung di peramban**: tidak ada yang perlu diteruskan web, dan pasangan CSRF yang dipakai persis pasangan yang sama (cookie `crk_csrf` ikut otomatis, tokennya naik sebagai `x-csrf-token` dari `data.csrf`). Langkah kedua digambar **di tempat** begitu API menjawab `mfaRequired` — fokus langsung ke kotak kode — dan berhasilnya ditutup `goto(next, { invalidateAll: true })`, jadi satu dokumen dari halaman masuk sampai dasbor. **Dasarnya tidak dicabut** (L-22): form `method="POST"` beserta action-nya tetap ada dan tetap dibuktikan proof HTTP; lebih dari itu, bila API tidak terjangkau — atau yang menjawab ternyata bukan API (mis. deployment tanpa rute `/v1` di depan web) — submit-nya **diserahkan kembali ke peramban** dan jalur tanpa JavaScript yang menyelesaikannya. Dua hal ikut berubah supaya lapis ini bisa berbahasa pengguna: refusal `/v1/auth/login/mfa` kini membawa `details.reason` (`bad_code` | `challenge_invalid` | `too_many_attempts`) sehingga **kedua** lapis tahu kapan tantangan dipertahankan tanpa membaca kalimat Inggris/Indonesia milik API (kebijakan pesan API tetap: web yang memetakan kode → kunci i18n, `auth.login.err.*` dan `auth.mfa.err.*`), dan kotak email dipasang `bind:value` — ekspresi `value={…}` ditulis ulang setiap kali halaman dirender ulang dan menghapus apa yang sedang diketik. Dibuktikan `apps/web/e2e/login.e2e.ts` di peramban sungguhan: penanda di `window` bertahan (berarti tidak ada muat ulang) dan satu-satunya POST adalah ke `/v1/auth/login`, kata sandi salah dijawab di halaman yang sama tanpa kehilangan email yang sudah diketik, dan — dengan `/v1` sengaja dijawab 404 — submit jatuh ke action halaman dan tetap masuk. Catatan: harness proof menjalankan API dan web sebagai dua proses telanjang tanpa proxy di depannya, jadi di sana yang teruji adalah jalur jatuh-balik itu

29. **Uji SMTP dari halaman Pengaturan** (J-1; diminta pemilik 2026-09-12) — **selesai**: satu-satunya cara membuktikan kredensial SMTP selama ini adalah `bun run mail:test` di shell, yang membaca `.env` **saja** — buta terhadap `mail.*` di database, dan mustahil bagi admin tenant yang tidak punya shell. Sekarang bagian **Email** menawarkan **Kirim email uji** (aksi section, titik perluasan 6) dengan kotak **Email tujuan** di sampingnya, terisi lebih dulu oleh **akun SMTP yang berlaku** untuk lingkup yang sedang dibuka — nilai yang hanya server bisa tahu, karena akun itu boleh datang dari setting *atau* dari `.env` (E-6), jadi API yang mengisinya (`smtpFor()`), bukan registry. `POST /v1/configuration/mail/test` (izin `config.edit`, `scope` ikut dikirim sehingga tombol di lingkup global menguji konfigurasi global, bukan tenant yang kebetulan aktif) mengirim **satu** email **langsung lewat SMTP, bukan lewat outbox** — pertanyaannya "apakah kredensial ini bekerja?", dan baris outbox baru menjawabnya semenit kemudian di halaman lain. Dua langkah seperti CLI-nya: `verify()` (koneksi, TLS, autentikasi) lalu kirim, sehingga kegagalan menyebut separuh mana yang patah, dengan `smtpHints()` memberi yang bisa dicoba (EAUTH → App Password, ESOCKET/"wrong version number" → mode TLS vs port, ETIMEDOUT → firewall VPS). Jawabannya selalu menyebut **apa yang barusan diuji** (host:port, mode TLS, akun, pengirim, tujuan) tanpa sandi, baik berhasil maupun gagal; tanpa SMTP sama sekali ia menjawab "belum dikonfigurasi", bukan galat permintaan. Karena ini satu-satunya tombol admin yang mengirim email ke alamat yang **diketik** pemanggil, ia punya jatah sendiri (10 per 10 menit per akun) dan tercatat di audit log (`mail.test`). Kontrak titik perluasan 6 ikut tumbuh: sebuah aksi section boleh mendeklarasikan **satu** `input` (kunci, label, tipe `text`/`email`, placeholder, prefill) yang dikirim di bawah nama field dari metadata — browser tidak pernah menamai field sendiri ([`MODULES.md`](./MODULES.md))

30. **Tool internal `core.get_current_datetime`** (titik perluasan 8; diminta pemilik 2026-09-14) — **selesai**: sampai sekarang setiap tool di registry adalah milik sebuah modul, jadi asisten AI tidak punya satu pun kemampuan yang bertahan ketika modul dinonaktifkan per tenant (G-8) — termasuk kemampuan paling dasar yang tidak dimiliki model bahasa mana pun: **mengetahui tanggal hari ini**. Tanpa itu setiap pertanyaan "berapa pesan bulan ini?" dijawab memakai tanggal karangan dari data latih. Core sekarang menyumbang **tool internal** sendiri (`apps/api/src/core-tools.ts`, namespace `core.`) yang ditandai `core: true` di registry dan karenanya melewati **satu** gerbang saja — pemeriksaan modul-aktif. Semua jaminan lain berlaku sama persis: butuh sesi + tenant aktif, `permission` ditegakkan bila dideklarasikan, argumen divalidasi terhadap skema, setiap panggilan tercatat `tool.call`, dan jalannya tetap satu — chat AI, `GET/POST /v1/tools`, dan `/v1/mcp` semuanya lewat `listTools`/`callTool` yang sama. Penghuni pertamanya `core.get_current_datetime` (kawat `core_get_current_datetime`, `readOnly`, **tanpa izin** — jam dinding bukan data tenant, dan tool yang tidak boleh dibaca adalah tool yang diganti model dengan tebakan): menjawab `iso`/`utc`/`epoch_ms`/`timezone`/`utc_offset`/`date`/`time`/`weekday`/`text` plus `ranges` — `today`, `yesterday`, `this_week`, `last_week`, `this_month`, `last_month`, `this_year`, `last_7_days`, `last_30_days`, masing-masing `{ from, to }` ISO-8601 **setengah terbuka** sehingga bisa langsung jadi filter laporan tanpa akal-akalan `23:59:59`. Yang membuatnya benar dan bukan sekadar `new Date()`: seluruh batas dihitung di **zona waktu aplikasi**, bukan UTC — baris disimpan dalam UTC (sesi MySQL dipaku ke `Z`), jadi "bulan ini" bagi pembaca di Asia/Jakarta mulai tujuh jam sebelum bulan UTC mulai. Zonanya konfigurasi baru **Aplikasi → Zona waktu** (`app.timezone`, per tenant seperti setting lain, baku = zona server/`TZ`), dengan argumen opsional `timezone` (IANA) untuk membaca zona lain; nama zona yang tidak dikenal ditolak dengan pesan yang bisa dipakai model, nilai konfigurasi yang rusak turun ke zona server alih-alih menjatuhkan tool. Minggu mulai **Senin** (ISO-8601), dan zona ber-DST ditangani: tiap batas membawa offset yang berlaku **pada batas itu** (`2026-03-01T00:00:00-05:00` … `2026-04-01T00:00:00-04:00`), hasil dua-lintasan `instantOf` — offset "sekarang" adalah offset yang salah untuk batas tiga minggu lagi. Logika murninya di `apps/api/src/lib/datetime.ts` (hanya `Intl`, tanpa dependensi) dengan 11 unit test `apps/api/test/datetime.test.ts` (Jakarta yang sudah berganti hari sementara UTC belum, pergantian bulan/tahun, hari 23 jam saat jam dimajukan), dan tes integrasi membuktikan anggota **tanpa izin apa pun** tetap bisa memanggilnya, argumennya tetap divalidasi (422), dan modul yang dinonaktifkan tidak ikut mematikannya ([`MODULES.md`](./MODULES.md) → Tool internal, [`AI.md`](./AI.md))

31. **Uji MCP & muat ulang tool lewat AJAX** (I-5, L-20 di atas L-22; diminta pemilik 2026-09-15) — **selesai**: tombol **Uji & muat tool** di `/m/ai/mcps/[id]` satu-satunya cara membuktikan sebuah server MCP hidup, tetapi ia memuat ulang seluruh halaman — form konfigurasi yang sedang diisi ikut digambar ulang, dan pada server yang lambat admin menatap halaman kosong selama sampai 20 detik (`MCP_TIMEOUT_MS`) tanpa tahu apakah permintaannya berjalan. Sekarang klik yang sama dikirim lewat `fetch` ke route JSON baru `POST /m/ai/mcps/[id]/test` (`modules/AI/web/routes/mcps/[id]/test/+server.ts`), yang memanggil **endpoint API yang sama persis** dengan form action-nya — `POST /v1/m/ai/mcps/:id/test`, yang memang sudah mengembalikan daftar tool lengkap — jadi lapis ini tidak menambah satu pun perilaku baru, hanya menghapus muat ulangnya. Polanya sama dengan uji penyedia AI (butir 25): **dasarnya tidak dicabut**, form `method="POST" action="?/test"` tetap ada dan tetap dirender lewat `form.tested` ketika JavaScript mati. Tiga hal yang ikut berubah supaya halamannya jujur tanpa muat ulang: (a) badge status di kepala halaman dan daftar tool sekarang **diturunkan** dari hasil uji terbaru, bukan dari apa yang kebetulan dimuat loader — jadi `ok`/`error`, jumlah tool, waktu sinkron, dan durasi probe berganti di tempat; (b) tombolnya **tahu ia sudah terhubung** — pada server ber-status `ok` ia berbunyi **Uji ulang & muat ulang tool**, karena menawarkan "uji" pada sesuatu yang sudah terbukti hidup menyembunyikan apa yang sebenarnya dilakukan tombol itu (mengganti daftar tool tersimpan dengan yang ada di sana sekarang); dan (c) uji yang **gagal** tidak mengosongkan tabel — persis seperti API, yang hanya menulis ulang baris tool bila koneksinya berhasil: menghapus daftarnya saat timeout berarti mengklaim server tidak punya tool padahal yang kita tahu hanya "tidak terjangkau barusan". Tiap baris tool kini juga menunjukkan badge **nonaktif** bila `ai_mcp_tools.enabled` mati, karena tool yang tidak ditawarkan ke asisten tidak boleh terlihat sama dengan yang ditawarkan. Dibuktikan `apps/web/e2e/mcp.e2e.ts` di peramban sungguhan melawan **server MCP sungguhan** — `modules/AI/scripts/mcp-mock-server.ts` (SDK resmi, Streamable HTTP stateless; ia tinggal di modul karena SDK itu dependensi modul AI, dan dijalankan `scripts/ci/m1-proof.sh` berdampingan dengan mock penyedia AI): penanda di `window` bertahan di seluruh uji, uji ulang, dan uji yang gagal (berarti tidak ada muat ulang), tabel terisi dua tool, label tombol berganti setelah terhubung, dan uji ke alamat mati menampilkan galat **tanpa** menghapus dua baris yang sudah ditemukan. Kasus tanpa JavaScript diuji di berkas yang sama dengan `javaScriptEnabled: false` — form action yang lama masih menghubungkan, menyimpan, dan melaporkan hasilnya. **Dilengkapi 2026-09-15 setelah dicoba pemilik:** (a) e2e sekarang **memaku** klaimnya alih-alih menyiratkannya — tidak ada navigasi main-frame sama sekali sesudah klik, satu-satunya POST ber-`resourceType` **`fetch`** (bukan `document`, yang justru tanda form biasa), tombolnya `disabled`, dan ikonnya benar-benar membawa `animate-spin`; mock MCP-nya sengaja dibuat lambat (`MOCK_MCP_DELAY_MS=400` di `scripts/ci/m1-proof.sh`) karena server yang menjawab 3 ms membuat keadaan "sedang berjalan" mustahil diamati, padahal justru itu yang harus dibuktikan tergambar. (b) **Pencarian tool** di sebelah kanan tombol uji: satu server bisa menawarkan puluhan tool yang namanya beda satu kata, dan jawaban atas "apakah `create_issue` masih ada?" tidak boleh berupa Ctrl-F. Barisnya **sudah seluruhnya ada di halaman** (API mengembalikan daftar penuh, tidak ada paging), jadi penyaringannya murni di klien — bukan permintaan lain — dan mencakup nama, nama kawat, serta deskripsi. Dasarnya tetap utuh: kotaknya field `method="GET"` sungguhan, tanpa JavaScript Enter memuat ulang halaman sebagai `?q=…` dan nilai awalnya dibaca balik dari URL sehingga HTML yang dirender server **sudah** tersaring (tombol kirimnya di dalam `<noscript>`, karena dengan JavaScript daftarnya sudah menyempit saat diketik). Penghitungnya ikut berganti jadi "N dari M tool" saat menyaring

32. **Simulasi modul repo terpisah + tes untuk dokumennya** (G-12, G-6; diminta pemilik 2026-09-16) — **selesai**: alur di [`Build-Module-for-Your-Apps.md`](./Build-Module-for-Your-Apps.md) sebelumnya hanya dijaga skrip CI berbasis shell dan gate cross-repo yang memakai nama modul `Xrepo` — sama panjang dengan `Hello` di template, dan berjalan di lingkungan tanpa `TABLE_PREFIX`. Dua kebetulan itu menyembunyikan dua cacat pada jalur yang justru ditulis di dokumen: (a) `bun run rename <Nama>` mengubah panjang identifier sehingga pemenggalan baris Biome bergeser dari template yang dibuat untuk `Hello`, dan karena repo modul tidak punya Biome sendiri (dependensinya hanya resolve di dalam core) `bun run harness` langsung merah di `biome check` untuk nama sepanjang `Contact` — atau `Billing`, contoh di dokumen itu sendiri; (b) `db:generate` menjalankan drizzle-kit yang mem-banding skema ter-generate (ikut `TABLE_PREFIX`, O-2) dengan snapshot ter-commit (tidak pernah ber-prefix), jadi dengan prefix ter-set **setiap** tabel terlihat berganti nama dan drizzle-kit berhenti menunggu jawaban interaktif yang tidak ada penjawabnya — migrasi modul diam-diam tidak pernah ditulis dan tes integrasinya jatuh di 500. Keduanya diperbaiki di sumbernya: harness memformat salinan modul di core (tempat Biome memang ada, persis seperti `modgen` memformat hasil generate-nya) lalu menulis hasilnya balik ke repo modul, dan prefix dilepas pada langkah generate di `packages/db` — sekali lagi di harness supaya modul tetap aman terhadap core versi lama — sementara migrasi tetap **diterapkan** dengan prefix. Yang menemukan keduanya: `bun run sim:module` (`scripts/module-sim.sh`), simulasi sekaligus tutorial yang menjalankan sembilan langkah dokumen untuk modul contoh `Contact` — direktori kerja, clone core, `.env` ter-generate, `bun create module` → `rename` → tag `v0.1.0`, harness (dengan `DATABASE_URL`: migrasi + CRUD sungguhan lewat tes integrasi), `modules:add` terkunci tag dari URL git, `db:generate` + migrate, lalu `modules:remove` sampai pohon core identik dengan HEAD lagi. Tiap langkah mencetak perintah yang akan diketik pembaca sebelum menjalankannya, dan tiap langkah diperiksa; tabel simulasi dipisah dengan `TABLE_PREFIX=sim_` dan dihapus lagi di akhir (juga di awal, supaya putaran yang mati di tengah tidak memblokir putaran berikutnya). Melengkapi sisi `bun test`-nya: `packages/module-kit/test/submodule.test.ts` (pin/drift/init submodule pada repo git sungguhan), `scripts/module-starter.test.ts` (kontrak template, `rename`, pagar `harness`), `scripts/modules-add.test.ts` (penolakan CLI sebelum apa pun terpasang). Ikut diperbaiki saat menjawab pertanyaan pemilik "kenapa harus ada langkah `rename`?": `bun create` menimpa `package.json` → `name` dengan nama folder tujuan (`mod-contact`, `Contact`), dan `rename` tidak bisa memperbaikinya karena string `@modules/hello` sudah lenyap sebelum ia jalan — sekarang `rename` menormalkan nama paket ke `@modules/<namespace>` apa pun nama foldernya. Satu langkah otomatis (`bun create module ../Contact` langsung jadi modul `Contact`) sempat dicoba lewat hook `bun-create.postinstall` dan **tidak bisa**: hook-nya memang dijalankan, tetapi perintah `bun …` di dalamnya diam-diam tidak jalan (`touch` jalan, `bun -e` tidak) — jadi urutan create → rename tetap, dan sekarang dijelaskan di dokumennya. Simulasinya juga menanyakan direktori kerja dan sumber core (klon repo lokal pada HEAD, atau klon GitHub pada `main`/`development`) sehingga pembaca bisa menguji branch yang benar-benar akan mereka clone. **Ditutup 2026-09-17** (saat mereview konsistensi README ↔ dokumen ini): klaim "ref wajib tag atau commit — branch ditolak" dulu hanya kalimat, karena `modules-add` cuma mewajibkan `--ref` **ada** sementara pin-nya `git checkout --detach` yang menerima nama branch dengan senang hati — dan ref yang berpindah membuat `modules.json` berhenti menggambarkan satu build (Q-5). Sekarang remote-nya sendiri yang ditanya, `git ls-remote --heads -- <url> <ref>`, sebelum submodule ditambahkan: ref yang di sana ternyata sebuah branch ditolak berikut perintah untuk melihat tag yang tersedia, dan ditolak pula bila ada tag bernama sama — yang ambigu bukan terkunci. Yang sengaja dibiarkan: `modules:sync` masih meloloskan nama branch yang **ditulis tangan** ke `modules.json`, karena ia berjalan di setiap build dan tidak boleh menambah panggilan jaringan

33. **Jam berjalan di bawah Zona waktu** (E-3; diminta pemilik 2026-09-16) — **selesai**: setting **Aplikasi → Zona waktu** (butir 30) adalah kotak teks berisi nama IANA, dan sebuah nama zona tidak menjawab pertanyaan yang sebenarnya diajukan admin saat mengisinya — "kalau saya pilih ini, jam berapa yang dipakai aplikasi?". Sekarang di bawah fieldnya ada **jam**: tanggal dan waktu yang berlaku di zona itu, plus nama zona dan offsetnya (`Asia/Jakarta · GMT+07:00`). Yang penting bentuknya, bukan jamnya: halaman Pengaturan **di-generate dari metadata** dan tidak boleh mengenal satu kunci pun (E-3), jadi jam ini menggantung pada **tipe field**, bukan pada `app.timezone` — tipe baru `timezone` di `ConfigFieldType`, yang berarti modul mana pun yang mendeklarasikan field bertipe itu mendapat jamnya tanpa menyentuh core. Tipe itu sekaligus membawa validasinya sendiri: sampai sekarang `app.timezone` hanyalah `string` dengan `max: 64`, sehingga "WIB" atau salah ketik tersimpan diam-diam dan baru terasa sebagai tanggal yang salah di laporan — `validateValue` kini menolak nama yang tidak dikenal `Intl`, dan kosong tetap berarti "ikuti global, lalu zona server". Jamnya **dirender server lebih dulu** (jadi benar tanpa JavaScript sama sekali, sama seperti sisa halaman ini), lalu berdetik sendiri tiap detik di peramban dan **membaca apa yang sedang diketik** — bukan yang tersimpan — sehingga zona bisa dicoba sebelum Simpan ditekan; nama yang tidak dikenal menampilkan pesan, bukan jam yang salah. Komponennya `apps/web/src/lib/components/TimezoneClock.svelte`, presentasional penuh (kata-katanya props, seperti `Presence`), angkanya `tabular-nums` supaya detiknya tidak menggoyang baris. Dibuktikan `packages/settings/test/validate.test.ts` (zona dikenal/tidak, kosong = warisi) dan `apps/web/e2e/settings-clock.e2e.ts` di peramban sungguhan (berdetik, ikut zona yang diketik, zona ngawur → pesan)

34. **Kirim "Hubungi kami" lewat AJAX** (R-5, L-20 di atas L-22; diminta pemilik 2026-09-16) — **selesai**: formulir kontak di landing `Example` memuat ulang seluruh halaman untuk satu pesan — pengunjung yang sudah menggulir melewati hero, produk, cerita, testimoni, dan harga dilempar balik ke dokumen baru, dan selama server bekerja tidak ada satu pun tanda bahwa tombolnya tertekan. Sekarang submit yang sama dikirim `use:enhance` ke **action yang sama persis** (`/example?/contact`): tidak ada endpoint baru, tidak ada perilaku server yang berubah — honeypot, rate limit 5/jam per IP, penyimpanan baris, dan email ke outbox semuanya tetap di jalur yang sudah dibuktikan gate M4 #3. Yang berubah hanya lapis atasnya: tombolnya berbunyi **Mengirim…** dengan ikon berputar selama permintaan berjalan, kegagalan digambar di tempat **tanpa menghapus apa yang sudah diketik** (`rate_limited` tetap dibedakan dari galat isian, lewat kode dari action — bukan kalimat API), dan berhasilnya mengganti formulir dengan kartu konfirmasi bercentang yang naik pelan (`prefers-reduced-motion` mematikan animasinya) berikut tombol **Kirim pesan lain** yang mengembalikan formulir kosong. **Dasarnya tidak dicabut** (L-22): `method="POST"` beserta action-nya tetap ada, dan klik yang mendarat sebelum hidrasi tetap mem-POST formulir seperti biasa — `data-enhanced` di form menandai lapis mana yang menjawab. Tombol "Kirim pesan lain" sengaja sebuah **tautan** ke `/example#contact`, sehingga jalan kembali itu juga ada tanpa JavaScript. Tiga kunci i18n baru di modul (`example.contact.sending`, `.sent_title`, `.again`) — tidak ada teks baru yang tertulis mati. Dibuktikan `apps/web/e2e/contact.e2e.ts` di peramban sungguhan: penanda di `window` bertahan (berarti tidak ada muat ulang), satu-satunya POST ber-`resourceType` **`fetch`** (bukan `document`, yang justru tanda form biasa), dan kasus tanpa JavaScript diuji di berkas yang sama dengan `javaScriptEnabled: false` — action lama masih menyimpan dan masih menampilkan konfirmasi ber-`data-testid="contact-sent"` yang dipakai gate M4

35. **Tanda terima ke pengirim formulir kontak** (R-5, J-1; diminta pemilik 2026-09-16) — **selesai**: satu kiriman formulir kontak menghasilkan email ke **tenant** (`example.contact_email`) dan lonceng ke pembaca inquiry, tetapi pengirimnya sendiri tidak mendapat apa pun kecuali kalimat di halaman — dan halaman itu hilang begitu tab ditutup. Sekarang pengunjung menerima **tanda terima** di kotak masuknya: template baru `contact-ack` (`packages/mail`), berisi sapaan bernama, satu kalimat bahwa pesannya diterima dan akan dibalas ke alamat itu, **salinan pesannya sendiri**, dan catatan bahwa email itu otomatis. Tiga keputusan yang membentuknya: (a) ia **tidak** bergantung pada `example.contact_email` — setting itu menentukan ke mana inquiry *dibaca*, sedangkan tanda terima adalah utang ke pengunjung, jadi etalase yang belum mengisi alamat kontak pun tetap membalas; (b) bahasanya mengikuti **permintaan yang mengirim form** (K-2) — `mailLocale()` sekarang dihitung sekali dan dipakai kedua email, jadi pengunjung berbahasa Inggris tidak dikirimi tanda terima berbahasa Indonesia; (c) ia lewat **outbox** seperti email lain (J-1/J-2), bukan SMTP di jalur request, sehingga kegagalan kirim tidak pernah menggagalkan pengiriman formulir. Soal penyalahgunaan: ini satu-satunya email yang dikirim ke alamat yang **diketik** anonim, jadi yang membatasinya adalah pagar formulir yang sudah ada — honeypot (bot dijawab "diterima" tanpa menyimpan **dan tanpa mengirim apa pun**) dan rate limit `example.inquiry_rate_limit` (baku 5/jam per IP); isi email pun hanya kata-kata pengunjung sendiri, yang di-escape seperti di template `contact`. Dibuktikan `packages/mail/test/templates.test.ts` (subjek dua bahasa, pesan dikutip balik, `<script>` ter-escape), `apps/api/test/integration/example.test.ts` (POST inquiry → baris outbox `contact-ack` ke alamat pengirim dengan `locale` mengikuti `Accept-Language`; honeypot tidak menghasilkan baris apa pun), dan gate M4 #3 yang kini ikut memeriksa baris tanda terima di outbox

36. **Merek email ikut `APP_LANDING_TITLE`** (J-3, F-6; diminta pemilik 2026-09-16) — **selesai**: `APP_LANDING_TITLE` sudah menamai halaman depan dan dokumen OpenAPI, tetapi email keluar masih memakai `app.name` dari Pengaturan (baku `Dashboard`), jadi deployment yang sudah bernama "Super App Dashboard" mengirim email ber-kop "Dashboard" — satu pesan terbaca sebagai produk lain dari situs yang mengirimnya. Sekarang nama itu diselesaikan satu fungsi murni, `resolveAppName()` di `apps/api/src/mail.ts`: `APP_LANDING_TITLE` (di-trim) menang, kosong/tidak diisi jatuh ke `app.name`, lalu `Dashboard`. Ia dipakai `brandFor()` — dan karena `Brand.appName` mengisi kop, baris footer, **dan** setiap subjek yang menyebut `{app}`, ketiganya berganti bersama; kop "Super App Dashboard" dengan subjek "— Dashboard" bukan perbaikan setengah, itu cacat baru. Ikut dipakai `smtpFor()` sebagai nama pengirim cadangan (hanya berlaku bila `mail.from_name` dan `MAIL_FROM_NAME` sama-sama kosong), supaya nama yang dibaca di daftar inbox pun produk yang sama. Konsekuensi yang disengaja dan dicatat di UI: untuk email, env **menimpa** setting per tenant — `APP_LANDING_TITLE` adalah nama deployment (F-6: harus benar sebelum database dibaca), jadi hint field **Nama aplikasi** di Pengaturan kini menyebutkannya. Dibuktikan `apps/api/test/smtp.test.ts` (env menang, kosong/spasi jatuh ke `app.name`, keduanya kosong → `Dashboard`) dan dirender langsung dengan `.env` pemilik: kop + subjek berbunyi "Super App Dashboard" ([`README.md`](../README.md), `.env.example`)

37. **`bun run systemd:install` — checkout jadi systemd user service** (Q-11; diminta pemilik 2026-09-17) — **selesai**: deploy manual ke produksi selama ini `git pull && bun run build && bun run start`, dan baris terakhirnya adalah proses biasa: tutup SSH, aplikasi mati; reboot, aplikasi tidak kembali. Dokumennya hanya menyarankan "pm2/screen/satu unit systemd" ([`DEPLOY.md`](./DEPLOY.md) §8e), yaitu menyuruh pembaca mengarang sendiri unit yang paling menentukan apakah situsnya hidup. Sekarang ada perintahnya: `bun run systemd:install` menulis `~/.config/systemd/user/crk.service` dari template ter-commit ([`deploy/systemd/user-app.service.template`](../deploy/systemd/user-app.service.template)) lalu enable + start, dan `bun run systemd:uninstall` mencopotnya lagi — jadi deploy berikutnya cuma mengganti baris terakhir: `git pull && bun run build && systemctl --user restart crk`. **User service, bukan `/etc/systemd/system`**: aplikasinya toh berjalan sebagai akun biasa dari checkout di `$HOME`, jadi start/stop/restart dan `git pull` tidak perlu sudo sama sekali, dan dua checkout (staging + produksi) bisa hidup berdampingan lewat `SERVICE_NAME=`. Ini **melengkapi**, bukan menggantikan, §8c yang ber-root dengan binary terkompilasi dan dua unit terpisah. Yang membuatnya bukan sekadar berkas unit yang ditempel: (a) **env-nya sama dengan yang dipakai dengan tangan** — `.env` menang karena itulah yang dimuat `bun run start` sendiri, `.env.prod` hanya bila cuma itu yang ada, `ENV_FILE=` mengalahkan keduanya, dan pilihannya selalu diteruskan eksplisit sebagai `--env-file=` supaya service tidak mewarisi tebakan direktori; bila keduanya ada, skripnya mengatakan mana yang dipakai — "jalan di terminal, aneh di service" adalah kegagalan yang paling mahal dicari. (b) **Artefak build diperiksa** dan `bun run build` dijalankan bila `apps/web/build`/`dist/web` belum ada, karena `bun start` memang menolak jalan tanpa itu. (c) **Preflight (Q-13) dijalankan sebagai laporan, bukan gerbang** — sengaja tidak jadi `ExecStartPre` seperti unit ber-root: dengan `Restart=always`, preflight yang gagal karena database belum siap akan berubah jadi lingkaran restart yang diam, sedangkan di sini admin membacanya sekali dengan mata sendiri; migrasi tetap langkah eksplisit (Q-4). (d) **Pemasangnya menunggu aplikasinya menjawab**, bukan sekadar "prosesnya ada": `systemctl restart` pulang begitu proses dijalankan, jadi skripnya mengetuk `HOST:PORT/v1/health` — host dan port dibaca dari env yang sama lewat `bun --env-file=… -e`, bukan dari angka baku yang ditulis mati — sampai 45 detik, menahannya 3 detik lagi, lalu memastikan unitnya masih hidup dan `NRestarts` tidak naik; dua kehalusan yang ketahuan saat diuji: jawaban 200 pertama bisa datang dari **gateway yang sudah mengangkat telepon padahal prosesnya sebentar lagi mati**, dan angka `NRestarts` harus dibaca **sesudah** restart karena systemd menolkannya pada start manual — membacanya sebelum restart membuat sisa crash-loop pemasangan lama terbaca sebagai kegagalan palsu. (e) **Log yang benar-benar bisa dibaca**: bakunya **berkas**, `logs/<nama-unit>.log` lewat `StandardOutput=append:` + `StandardError=append:`, bukan journal — karena ada host (AlmaLinux 9 milik pemilik, tempat ini diuji) yang tidak memberi akun biasa akses baca ke journal-nya sendiri sehingga `journalctl --user` menjawab *insufficient permissions* dan log aplikasi praktis hilang, sedangkan berkas selalu bisa dibaca pemiliknya; `LOG_FILE=journal` mengembalikannya ke journald bagi yang punya aksesnya (dan skripnya tetap memperingatkan bila ternyata tidak terbaca), `LOG_FILE=/path/lain.log` memindahkan berkasnya, rotasinya bukan urusan systemd sehingga disediakan [`deploy/systemd/logrotate.example`](../deploy/systemd/logrotate.example) ber-`copytruncate` (rename tidak cukup: prosesnya memegang berkas yang sama sepanjang hidupnya), dan `systemd:uninstall` sengaja tidak menghapus lognya — biasanya justru itu yang dicari sesudah sebuah service dicopot. (g) **Port diperiksa milik siapa sebelum apa pun ditulis**: ketiga port (`PORT`, `API_PORT`, `WEB_PORT_INTERNAL`) diketuk, dan yang dipegang proses lain menghentikan pemasangan — pelajaran dari pemasangan uji yang bentrok dengan unit `crk` milik pemilik yang sedang jalan: bentrokan port di sini **tidak** selalu berbunyi keras, gateway-nya memang gagal bind lalu crash-loop tetapi proses api-nya justru **berbagi** port dengan tetangganya (SO_REUSEPORT — dua instance menjawab `/v1/health` bergantian, teramati langsung), jadi tanpa pagar ini akibatnya cuma "aneh separuh waktu"; kepemilikan dibaca dari `ss` + cgroup unit sehingga re-install unit yang sama tetap lolos. (f) **Linger** diperiksa (`loginctl show-user`) dan disebut, karena tanpa `enable-linger` sebuah user service mati saat sesi login terakhir tertutup — persis kegagalan yang hendak dihindari. Unitnya sendiri mengikuti kontrak `bun start`: `KillSignal=SIGTERM` + `TimeoutStopSec=35` (25 s yang diberikan `start.ts` ke kedua anaknya, plus ruang), `KillMode=mixed`, `Restart=always`, `LimitNOFILE=65536`, dan `PATH` eksplisit karena unit systemd tidak membaca `~/.bashrc`. `--dry-run` mencetak unit tanpa menyentuh systemd. Diuji di host sungguhan: pasang → gateway menjawab di port dari `.env` (`/v1/health` 200, tiga proses di cgroup unit), pasang ulang (idempoten), mode `LOG_FILE`, lalu copot sampai unit, symlink `default.target.wants`, dan portnya bersih lagi ([`DEPLOY.md`](./DEPLOY.md) §8f)
38. **Setel kata sandi & kirim tautan reset dari detail pengguna** (D-1, A-7; diminta pemilik 2026-09-21) — **selesai**: sampai kini satu-satunya jalan admin menolong anggota yang terkunci adalah menghapus 2FA-nya (A-11) atau menyuruhnya ke halaman *Lupa kata sandi* — padahal akun yang kotak masuknya tidak terjangkau (akun bersama, e-mail lama, fixture) tidak pernah menerima tautan itu. Halaman `/users/[id]` kini punya kartu **Kata sandi** untuk pemegang `user.edit` dengan dua jalan: **setel langsung** (`PUT /v1/users/:id/password`, kata sandi diketik dua kali, aturan kekuatan yang sama dengan profil, form AJAX progresif lewat `FormBuilder ajax` — tanpa JavaScript tetap POST biasa) dan **kirim tautan reset** (`POST /v1/users/:id/password/reset-link`, satu klik, token A-7 berlaku 24 jam seperti akun baru karena bukan admin yang akan membukanya; akun tanpa kata sandi mendapat wording `set-password`, yang sudah punya mendapat `reset-password`; bahasa surat mengikuti `users.locale` penerima dulu, K-2 hanya bila belum memilih). Menyetel kata sandi mengakhiri **semua** sesi anggota itu (bukan "sesi lain" seperti di profil — yang memegangnya masuk dengan kata sandi yang sudah bukan miliknya) dan menandai token reset yang masih menggantung sebagai terpakai, supaya surat lama tidak menimpa keputusan admin. Pagar yang sama dengan `PUT /:id`: hanya anggota tenant aktif (id tenant lain = 404), superadmin hanya oleh superadmin, ditolak saat impersonasi (`notImpersonating`, D-6), plus satu miliknya sendiri — **bukan untuk diri sendiri** (409; profil meminta kata sandi lama lebih dulu, rute ini tidak), jadi kartunya juga tidak tampil di halaman akun sendiri. **Tautan tidak dikirim ke alamat `.test` / `.invalid`** (RFC 2606, tidak pernah menerima surat): aturannya satu fungsi, `isUndeliverableEmail` di `@core/contracts`, dipakai API (422 `validation_failed`, `details.reason = email_undeliverable` — kode error yang ada, alasannya yang baru, sesuai kebijakan pesan API tetap Indonesia dan web memetakan `details.reason` ke i18n) dan web (tombolnya diganti kalimat penjelas, bukan tombol mati). Kegagalan aksi membawa `scope` (`password` / `resetLink`) supaya pesan galat kartu ini tidak nyasar ke form profil di atasnya — sebelumnya `form.error` dipakai bersama oleh semua aksi di halaman itu. Diaudit `user.password_set_by_admin` dan `user.password_reset_link`. Dibuktikan `apps/api/test/integration/admin-password.test.ts` (401/403/404/409, 422 lemah tanpa efek samping, sesi target mati + login lama gagal + token menggantung terpakai + audit, 422 `.test` tanpa baris outbox, tautan: template/bahasa/TTL/token/audit) dan `packages/contracts/test/email.test.ts` (sufiks tepat dua, huruf besar/spasi/subdomain, domain yang hanya *mengandung* kata `test`/`invalid` tetap lolos)
39. **Tampil/sembunyikan kata sandi di halaman masuk & chat mengambang layar penuh di ponsel** (A-2, H-13; diminta pemilik 2026-09-22) — **selesai**: (a) field kata sandi di `/auth/login` mendapat tombol mata (ikon semantik `eye`/`eye-off`, L-5) yang mengganti `type` antara `password` dan `text` tanpa menyentuh nilai atau `name` field — tombolnya **hanya dirender setelah hidrasi** (`enhanced`), karena tanpa JavaScript tidak ada yang bisa ia lakukan dan tombol mati di samping field lebih buruk daripada tidak ada (L-22 tetap: form dasar tidak berubah); label berada di `<label for>` terpisah karena `<button>` tidak boleh berada di dalam `<label>` yang sudah melabeli sebuah input; teksnya lewat i18n (`auth.login.show_password`/`hide_password`, `aria-pressed` mengikuti keadaan); halaman daftar/reset/undangan **belum** mendapat tombol yang sama (daftar menyusul di butir 41, undangan dan reset di butir 44). **Catatan (2026-09-22):** versi pertama memberi halaman ini blok `<style>` untuk ruang tombol — dan bingkai input serta tombol masuk **hilang**: field-field auth mengambil rupanya dari `app.css` sebagai elemen **tanpa class** (`input:not([class])`, `button:not([class])`), sedangkan scoping Svelte menempelkan class hash pada elemen yang dijangkau selektor (di dev, lewat vite-plugin-svelte + HMR, pada **semua** elemen komponen). Blok gaya dibuang; ruangnya jadi `style` inline pada input. Pelajaran umum: halaman yang mengandalkan `:not([class])` tidak boleh punya `<style>`. `login.e2e.ts` kini memeriksa bingkai 1px kedua input dan latar tombol. (b) panel `FloatingChat.svelte` di bawah breakpoint `sm` (640px) menjadi **layar penuh** (`fixed inset-0`, tanpa sudut membulat dan bingkai) dan tombol bundar pelontarnya disembunyikan selama panel terbuka — menutup adalah tugas tombol × di header; baris tautan paling bawah memberi ruang `env(safe-area-inset-bottom)`. Dari `sm` ke atas kartu pojok yang lama tidak berubah. Dibuktikan `apps/web/e2e/login.e2e.ts` (toggle: `type` berganti, nilai tetap, tidak submit) dan `apps/web/e2e/floating-chat-mobile.e2e.ts` (viewport 390×844: panel = seluruh viewport, pelontar hilang, header menutup).
40. **Daftar pengguna: filter grup, pencarian AJAX, fokus tetap di kotak cari** (D-1, L-16, L-20 di atas L-22; diminta pemilik 2026-09-23) — **selesai**: (a) `GET /v1/users` menerima `?group=<id>` — anggota grup itu di tenant aktif, lewat subquery `group_user_maps` yang ber-`client_id`; id grup tenant lain atau grup terhapus bukan galat, hanya tidak mencocokkan siapa pun. Halaman `/users` menawarkan `<select name="group">` di samping kotak cari (opsi dari `GET /v1/groups`, maks 100, hanya bila pemanggil punya `group.read` — tanpa itu filter dari URL tetap berlaku lewat input tersembunyi). (b) Lapis ter-enhance kini ada **di `DataTable` sendiri**, jadi `/groups`, `/tenants`, dan daftar modul mendapatkannya bersama-sama: ketiga form GET (cari + filter, pemilih kolom, ukuran halaman) di-submit lewat `goto()` dengan `keepFocus` + `noScroll` — mengetik mencari sendiri (debounce 300 ms, `replaceState` supaya satu ketikan bukan satu entri riwayat), select filter dan ukuran halaman berlaku saat `change`, pemilih kolom menutup sendiri sesudah diterapkan, dan tabel meredup ber-`aria-busy` selagi fetch berjalan. Enter maupun klik tombol **Cari** mengembalikan fokus ke kotak `q`; kotaknya adalah state lokal yang **tidak** ditimpa nilai URL selagi berfokus, supaya jawaban pencarian yang datang terlambat tidak memakan huruf yang sudah diketik. **Dasarnya tidak berubah**: markup-nya form GET yang sama, URL yang dihasilkan JS identik byte demi byte dengan yang dibangun peramban tanpa JS, dan `data-enhanced` di form menandai lapis mana yang menjawab. State tabel mendapat `extra` (`tableStateFrom(url, { extra: ['group'] })`): filter khusus halaman yang ikut di setiap tautan urut/paginasi dan setiap form, sehingga mengurutkan tidak pernah menjatuhkan filter yang aktif. Dibuktikan `apps/api/test/integration/admin.test.ts` (filter grup mengembalikan satu anggota; id acak → nol), `apps/web/src/lib/components/table/columns.test.ts` (`extra` dibaca hanya untuk kunci yang disebut dan ikut `withParams`), dan `apps/web/e2e/users.e2e.ts` di peramban sungguhan: penanda di `window` bertahan sepanjang ketik → Enter → klik tombol → ganti grup → urut → reset, dan kotak `q` tetap berfokus sesudah Enter maupun klik
40. **Tab `/settings` satu baris yang bisa digeser di layar sempit** (D-2, F-1; diminta pemilik 2026-09-22) — **selesai**: baris tab pengaturan tidak lagi melipat (`flex-wrap`) menjadi beberapa baris di peramban ponsel — ia tetap **satu baris** (`flex-nowrap`, setiap tab `shrink-0 whitespace-nowrap`) dan **bergeser mendatar** (`overflow-x-auto`, scrollbar tipis di desktop); pembungkusnya `min-w-0` karena `.page` adalah grid dan jalur `auto` membesar mengikuti lebar min-content barisnya — tanpa itu yang bergeser adalah halamannya, bukan barisnya. Tab yang aktif — dari klik, `?tab=`, atau hash — di-`scrollIntoView` secara mendatar saja (`inline: nearest`, `block: nearest`) sehingga tautan langsung ke tab paling kanan tidak mendarat pada baris yang terlihat kosong; halamannya sendiri tidak melompat. Dibuktikan `apps/web/e2e/settings-tabs-mobile.e2e.ts` pada viewport 390×844: semua tab sejajar di satu garis, `scrollWidth > clientWidth`, memilih tab terakhir menggeser barisnya tanpa menggulir halaman.
41. **Tampil/sembunyikan kata sandi di daftar, profil, dan detail pengguna** (A-2, D-1; diminta pemilik 2026-09-22) — **selesai**: tombol mata dari butir 39 dijadikan komponen bersama `<PasswordInput>` (`apps/web/src/lib/components/ui/PasswordInput.svelte`): mode `plain` untuk halaman auth (input tanpa class, rupa dari `app.css`; ruang tombol lewat `style` inline — komponen ini sengaja **tanpa** blok `<style>`, lihat catatan butir 39) dan mode baku yang membungkus `<Input>` dasbor. Halaman masuk dan **daftar** (`/auth/register`) memakainya langsung; **profil** (`/profile`, ganti kata sandi: sekarang/baru/konfirmasi) dan **detail pengguna** (`/users/[id]`, setel kata sandi: baru/konfirmasi) mendapatkannya lewat `FormBuilder`, yang kini merender setiap field `type: 'password'` dengan komponen ini — berlaku juga untuk `/users/new` dan modul mana pun yang memakai FormBuilder. Tombol hanya dirender setelah hidrasi (tanpa JavaScript = field biasa), `aria-pressed` + `aria-controls` mengikuti field-nya, label lewat `common.show_password`/`hide_password` (kunci dipindah dari `auth.login.*` agar tersedia di seluruh namespace). Dibuktikan `apps/web/e2e/password-toggle.e2e.ts` (profil: tiga field, tiga tombol, satu tombol hanya membuka field-nya sendiri, tidak ada submit) dan `login.e2e.ts` (mode `plain`).
42. **Halaman depan bawaan jadi landing korporat** (F-6, §4.7; diminta pemilik 2026-09-22) — **selesai**: halaman `/` yang disajikan core sendiri — saat `app.landing_route` kosong atau berisi `/`, atau saat route pilihan tidak bisa dirender (modul nonaktif, route hilang) — selama ini hanya satu judul, satu baris versi API, dan tautan *Masuk*: sebagai pintu depan cadangan ia "valid" menurut F-6, tetapi sebuah deployment yang memang tidak punya sisi publik (landing diarahkan ke `/`) tampil seperti halaman uji. Sekarang ia landing korporat utuh dengan susunan yang sama dengan landing `Example`: hero (judul dari `landing.title`/`APP_LANDING_TITLE`, tagline, CTA masuk/daftar/dokumentasi API, tiga angka nyata — modul terpasang, bahasa antarmuka, tema bawaan), pratinjau dasbor yang **digambar dari token tema** (rail, header, tile KPI, grafik batang — tanpa gambar, tanpa permintaan aset), strip lima prinsip produk (§1 BRIEF), tujuh kartu fitur platform (yang ketujuh, *Pelacakan email*, ditambahkan 2026-09-25 setelah J-6 selesai — §8 #50), yang sejak 2026-09-25 bergeser otomatis ke samping **dan bisa digeser manual**: tanpa JavaScript barisnya strip scroll ber-snap (`.marquee` di `app.css`, satu salinan, geser sentuh/trackpad); action `use:autoSlide` (`$lib/actions/auto-slide.ts`) menambahkan autoplay satu kartu per 3,5 detik, drag dengan mouse, dan putaran tanpa sambungan (daftar digambar dua kali, salinan kedua `aria-hidden`, posisi dilipat balik satu periode lewat `wrapOffset()` setelah scroll berhenti). Autoplay berhenti saat hover/fokus/tab tersembunyi dan menunggu 6 detik setelah pengguna menggeser; arah mengikuti RTL; `prefers-reduced-motion` → tanpa autoplay, tetap bisa digeser, **status instalasi** dari `GET /v1/version` (nama@versi API, dialek, commit, dan daftar modul terpasang — `data-testid="api-status"` dipertahankan; API yang tidak terjangkau menampilkan kartu peringatan, bukan halaman kosong), tiga langkah developer dengan perintahnya, dan pita CTA penutup. Aturannya sama dengan R-7 untuk modul: **tanpa warna literal** (hanya token — teruji di `base` terang dan `corporate` gelap), **tanpa teks literal** (55 kunci `landing.*` baru di kedua katalog; `landing.lead` yang masih berbunyi "hadir di M4" ikut diperbarui karena ia tampil di footer publik dan panel `split-hero`), **tanpa state dan tanpa JavaScript** di jalur render, dan **core tidak menyebut nama modul mana pun** — daftar modul murni dari API. Metadata SEO lengkap (title, description, canonical, Open Graph, JSON-LD `WebSite`). Cek gate M4 #4 (Example dinonaktifkan → `/` jatuh ke halaman bawaan) kini menuntut `data-testid="landing-hero"` dan `api-status` ada di HTML, bukan sekadar 200 tanpa etalase. Diverifikasi render di 1366 px dan 390 px, cabang API terjangkau (tiga modul terdaftar) maupun tidak (503), tanpa galat konsol ([`README.md`](../README.md))
43. **Pilih tool per server MCP dari halaman detail** (I-5, L-20 di atas L-22; diminta pemilik 2026-09-24) — **selesai**: kolom `ai_mcp_tools.enabled` sudah ada sejak butir 2 dan tool source sudah melewati baris yang mati, tetapi **tidak ada satu pun jalan** untuk mematikannya — admin hanya bisa menawarkan seluruh server atau tidak sama sekali, padahal server MCP umum (GitHub, basis data, sistem berkas) menawarkan puluhan tool yang sebagian tidak boleh disentuh asisten di tenant itu. Sekarang tiap baris di tabel tool `/m/ai/mcps/[id]` punya **kotak centang** (bawaan aktif — tool baru dari server ikut aktif), kotak di kepala tabel **memilih/melepas semua** baris yang **tampil** (di bawah pencarian berarti hanya yang tersaring, karena itulah yang dilihat orangnya), dan penghitung di atas tabel menambah "N aktif". Di sebelah pencarian ada **filter status** (Semua / Hanya aktif / Hanya nonaktif) — `<select name="status">` di form GET yang sama, dibaca balik dari `?status=` sehingga HTML server sudah tersaring, dan bersama kotak kepala tabel inilah cara "matikan semua kecuali…" atau "hidupkan lagi yang tadi kumatikan" dikerjakan. **Tidak ada tombol simpan**: kotak yang berubah langsung menyimpan dirinya lewat `fetch` ke route JSON `POST /m/ai/mcps/[id]/tools` (`modules/AI/web/routes/mcps/[id]/tools/+server.ts`), yang memanggil endpoint API baru `PUT /v1/m/ai/mcps/:id/tools` dengan `{ ids, enabled }` — satu keputusan untuk satu himpunan, jadi satu klik = satu permintaan dan "pilih semua" juga satu. Jawabannya adalah **daftar tool terkini** dari server, dan halaman menggambar ulang dari jawaban itu — bukan dari apa yang ia minta — sehingga id basi sesudah uji ulang (yang mengganti semua baris) tidak menyesatkan; pilihan digambar optimistis lebih dulu, dan hanya jawaban **terakhir** yang diterapkan supaya dua klik cepat yang jawabannya tiba terbalik tidak saling menghapus. Dasarnya tetap utuh (L-22): tabelnya satu form `?/tools`, tiap baris mengirim id-nya dan baris tercentang mengirimnya lagi sebagai `checked`, tombol Terapkan hanya ada di `<noscript>`, dan form action-nya memanggil endpoint API yang sama. Di API: hanya baris milik server itu (dan tenant itu) yang tersentuh — id dari server lain diam saja, `ids` kosong 422, tanpa `ai.mcp.manage` 403 — setiap keputusan diaudit `ai.mcp.tools`, dan tool yang nonaktif hilang dari registry, `/v1/tools`, `/v1/mcp`, dan daftar tool yang dikirim ke model, sementara tool lain dari server yang sama tetap ada. Dibuktikan `modules/AI/test/integration/mcp-client.test.ts` (matikan `echo` saja → `weird name/v2` tetap ditawarkan, `echo` 404 saat dipanggil, model tidak lagi menerimanya, uji ulang mengganti id tetapi mempertahankan `enabled=false`, id basi mengubah 0 baris, server tenant lain 404, pilih semua mengembalikan bawaan, audit tercatat) dan `apps/web/e2e/mcp.e2e.ts` di peramban sungguhan: satu-satunya POST sesudah klik adalah `fetch …/tools`, tidak ada navigasi, badge **nonaktif** dan penghitung berganti di tempat, kotak kepala jadi *indeterminate* saat campuran, pilihan bertahan lewat muat ulang dan uji ulang, pilih semua di bawah pencarian tidak menyentuh baris tersembunyi, dan jalur tanpa JavaScript menerapkannya lewat tombol `<noscript>` ([`MCP.md`](./MCP.md), PRD I-5)
44. **Konfirmasi kata sandi & tombol tampil/sembunyikan di halaman undangan** (A-13, A-2; diminta pemilik 2026-09-24) — **selesai**: form `/auth/join/[code]` sampai kini hanya punya satu field kata sandi biasa — orang yang mengetik salah satu huruf membuat akun dengan kata sandi yang tidak ia kenal, dan satu-satunya jalan pulih adalah alur lupa kata sandi. Kini kedua field (kata sandi dan **ulangi kata sandi**, `name="password_confirm"`) dirender lewat `<PasswordInput plain>` dari butir 41 — tombol mata per field, hanya sesudah hidrasi, label lewat `<label for>` seperti halaman masuk/daftar. Kecocokannya diperiksa di **form action web** sebelum API dipanggil (pola yang sama dengan ganti kata sandi di `/profile`): tidak cocok → 422 `password_mismatch` dengan pesan `join.password_mismatch`, nama tetap terisi, field konfirmasi diberi `aria-invalid`, dan **API tidak pernah melihat field konfirmasi** — kontrak `POST /v1/auth/join` tidak berubah. Jalur tanpa JavaScript utuh (L-22): dua field biasa, cek server. Dibuktikan `apps/web/e2e/join.e2e.ts` di peramban sungguhan: undangan dibuat dari `/users`, tautannya dibuka di konteks tamu, dua tombol mata yang masing-masing hanya membuka field-nya, konfirmasi berbeda ditolak di halaman yang sama, konfirmasi cocok membuat akun dan membuka sesi. **Halaman reset kata sandi (`/auth/reset`, A-7) menyusul pada hari yang sama** dengan bentuk yang persis sama (dua `<PasswordInput plain>`, `password_confirm`, cek di form action → 422 `password_mismatch` / `auth.reset.mismatch`, kontrak `POST /v1/auth/reset-password/confirm` tak berubah) dan teksnya kini lewat i18n (`auth.reset.*`, sebelumnya hard-coded Indonesia). Saat membuktikannya ketemu **bug lama**: `load` halaman itu memeriksa token lewat POST ke API, tetapi cookie CSRF baru dicetak *sesudah* panggilan itu — di peramban segar (tautan dari e-mail hampir selalu kunjungan pertama) API menjawab 403 `token_missing` dan halaman selalu berkata "tautan tidak valid"; kini `csrfToken(event)` dipanggil lebih dulu. Karena token sekali-pakai itu tidak pernah tampil di halaman mana pun, buktinya bukan Playwright melainkan `scripts/reset-form-proof.ts` (HTTP polos, tag `RESET` di `scripts/ci/m1-proof.sh`): akun sekali pakai dibuat lewat `/users/new`, barisnya ditanam ke `password_reset_tokens` seperti uji integrasi API, kunjungan pertama tanpa cookie merender form, HTML tanpa tombol toggle sebelum hidrasi, konfirmasi berbeda → 422 dan token tetap berlaku, konfirmasi cocok → 303 `/auth/login?reset=1`, token habis, kata sandi baru masuk dan yang lama ditolak.
45. **Grup Regular User tidak lagi bisa melihat daftar pengguna** (O-3, C-3; diminta pemilik 2026-09-24) — **selesai**: grup sistem `user` selama ini di-seed dengan `user.read`, sehingga setiap akun yang mendaftar sendiri atau dibuat admin dengan grup bawaan bisa membuka `/users` dan melihat seluruh anggota tenant — wajar untuk dasbor internal, salah untuk deployment yang menghadap pelanggan (e-commerce, portal): seorang pembeli tidak boleh tahu siapa pembeli lainnya. Kini `SYSTEM_GROUPS` di `packages/auth/src/seed.ts` memberi grup itu **daftar izin kosong**; Administrator tetap `*.*`, dan izin untuk anggota biasa ditambahkan admin lewat editor grup sesuai kebutuhan deployment-nya. Hanya seed yang diubah, **tanpa migrasi data**: seed bersifat tambah-saja (idempoten, O-3), jadi database yang sudah di-seed tetap memegang `user.read` sampai admin mencabutnya sendiri di `/groups` — diputuskan pemilik karena aplikasi masih internal dan database dev direset manual. `scripts/m1-gate1-proof.ts` ikut diubah: langkah 2–4 dahulu **menulis ulang izin grup `user` yang di-seed** (`user.read` + `group.read`), yang akan mengembalikan celah ini di setiap database yang pernah menjalankan gate; kini ia membuat grup sekali pakai `gate1-<run>` lewat `/groups/new` dan bekerja di sana, sementara cek bahwa `/users/new` menampilkan grup "Regular User" dipertahankan. `packages/auth/test/effective.test.ts` (seed hanya wildcard/izin terdaftar) dan `apps/api/test/integration/tenancy.test.ts` (pendaftar baru tanpa izin sampai digrupkan) sudah memakai grup buatan sendiri, jadi tidak terpengaruh.

46. **Pilih grup saat mengundang lewat email** (A-13, C-3; diminta pemilik 2026-09-24) — **selesai**: undangan dari `/users` sampai kini hanya membuat akun dan keanggotaan tenant — orang yang menerima tautan mendarat **tanpa grup sama sekali**, berbeda dari `/users/new` yang menawarkan kotak centang grup, dan admin harus membuka profilnya sesudah ia bergabung untuk memberi grup. Kini form undangan punya **pilihan grup** (`<select name="groupId">`, `data-testid="invite-group"`) yang **bawaannya Regular User** (grup sistem `user` dari seed) dengan opsi *tanpa grup*; kolom **Grup** ditambahkan di tabel undangan menunggu. Di API: `POST /v1/invitations` menerima `groupId` (dihilangkan → grup `user` tenant aktif bila ada; `null` → tanpa grup; id → harus grup hidup tenant itu, selain itu 422 `validation_failed` sebelum baris atau email mana pun dibuat), kolom baru `invitations.group_id` (FK `groups`, `ON DELETE SET NULL`; migrasi **0028** mysql/pg dan **0001** sqlite, semuanya `ALTER TABLE … ADD` — aditif, aman untuk rollout berdampingan), respons `GET`/`POST` memuat `group: { id, name } | null`. Grup diberikan **saat undangan diterima** (`POST /v1/auth/join`): baris `group_user_maps` di `client_id` undangan — grup yang sudah dihapus sebelum tautan dipakai sekadar dilewati, undangannya tetap sah. Alamat yang **sudah terdaftar** (jalur "ditambahkan ke tenant") langsung mendapat grup itu, **tanpa melepas** grup yang sudah ia pegang (tambah-saja, berbeda dari `PUT /v1/users/:id` yang mengganti). Semua keputusan tercatat di audit (`groupId` pada `invitation.create`, `invitation.existing`, `auth.join`). Sisi web: picker hanya tampil bila pemanggil punya `group.read` (daftar grup yang sama dengan filter tabel); tanpa itu field tidak dikirim dan API memakai bawaan. **Mengundang bertanya dulu** (L-22, pola dua-POST yang sama dengan nonaktifkan massal): POST pertama tanpa `?confirm=invite` hanya mengembalikan `confirmInvite` dan halaman menampilkan pertanyaan inline (alamat + grup yang akan diberikan) dengan form langkah kedua; dengan JavaScript submit dicegat dan pertanyaan yang sama dibuka di modal (`data-testid="invite-confirm-dialog"`) — keduanya mengirim ke `?/invite&confirm=invite`, dan tanpa token itu tidak ada e-mail yang keluar. Input email, pilihan grup, dan tombol kirim kini setinggi `h-9` (tombol memakai ukuran bawaan, bukan `sm`). Dibuktikan `apps/api/test/integration/invitations.test.ts` (bawaan Regular User di respons, daftar, dan pada akun yang bergabung; grup pilihan; tanpa grup; id asing 422 tanpa baris; grup dihapus sebelum diterima; alamat terdaftar mendapat grup dan undangan ulang tidak menggandakannya) dan `apps/web/e2e/join.e2e.ts` (opsi terpilih "Regular User", opsi tanpa grup ada, kolom Grup di tabel undangan, akun yang bergabung tampil dengan grup itu di tabel pengguna)

47. **URL favicon kustom di Pengaturan → Aplikasi** (E-1, L-24; diminta pemilik 2026-09-24) — **selesai**: sampai kini favicon hanya bisa diganti dengan mengganti berkas `static/favicon.svg` (butuh build) atau mengunggahnya ke sebuah tema kustom (butuh tema kustom aktif); logo sudah lama punya jalan ketiga lewat `app.logo_url`, favicon belum. Kini ada kunci `app.favicon_url` (`string`, publik, maks 512, per tenant dengan fallback global) tepat **di bawah `app.default_theme`** di halaman `/settings` (semula di bawah `app.home_route`; dipindah pada hari yang sama atas ralat pemilik), dan `<head>` root layout memakainya sebagai `<link rel="icon">` dengan urutan favicon tema kustom aktif → `app.favicon_url` → favicon bawaan; nilai yang bukan URL absolut atau path (`usableAsset`) diabaikan seperti pada logo. Urutan `order` tiga field sesudahnya bergeser satu. Dibuktikan `scripts/m3-gate-proof.ts` (E-1: favicon dari konfigurasi tampil di shell dasbor, mengosongkannya mengembalikan `/favicon.svg`) dan `apps/api/test/integration/config.test.ts` (field ada, bertipe `string`, persis di bawah `app.default_theme`). PRD Keputusan I dan [`THEMES.md` §5a](./THEMES.md) ikut diperbarui
48. **IP klien asli di belakang NAT / rantai proxy** (A-2, D-5, M-1; diminta pemilik 2026-09-24) — **selesai**: di produksi yang berada di belakang NAT, kolom *Last Login IP* dan *Last Active IP* (juga rate limit per IP dan `ip` di audit log) berisi alamat jaringan lokal, bukan IP pengunjung. Penyebabnya dua pembaca IP yang sama-sama rapuh terhadap hop tambahan: api mengambil **entri pertama** `X-Forwarded-For`, web mengandalkan `getClientAddress()` SvelteKit yang membaca **satu kedalaman tetap** (`XFF_DEPTH`) — begitu ada satu proxy/NAT lagi di depan, yang terbaca adalah alamat LAN hop tersebut. Kini keduanya memakai **satu aturan** di `@core/contracts` (`clientIpFromForwarded`, diuji unit): baca `X-Forwarded-For` dari kanan, lewati alamat privat/loopback/link-local/CGNAT (IPv4 dan IPv6, dengan port dan bentuk `::ffff:` dinormalkan), ambil alamat publik pertama; bila semua privat (pengunjung LAN, tes integrasi yang memakai `10.99.x.y`), entri paling kiri; tanpa header, alamat socket. Aturan ini tidak butuh konfigurasi kedalaman dan lebih tahan pemalsuan daripada "entri pertama" (alamat yang ditambahkan proxy terluar diperiksa lebih dulu). Web membaca header hanya bila `ADDRESS_HEADER` disetel (sudah baku di `bun start`, compose, dan env systemd), selebihnya tetap alamat socket. Batasnya tetap: proxy terluar harus **menulis** `X-Forwarded-For` — Caddy bawaan membuang header dari klien tak dipercaya (pakai `Caddyfile.behind-proxy` / `trusted_proxies`), dan proxy TCP murni atau NAT *masquerade* tidak membawa header sama sekali; keduanya dicatat di [`DEPLOY.md`](./DEPLOY.md) §2b, §8e, dan tabel pemecahan masalah.
49. **BCC global, uji SMTP tercatat di outbox, kirim ulang semua email** (J-1, J-2; diminta pemilik 2026-09-25) — **selesai**: tiga kekurangan kecil di jalur email. (1) Belum ada cara mengarsipkan seluruh email keluar: kini `MAIL_BCC` di `.env` (divalidasi sebagai alamat email, hanya `.env` — aturan deployment, bukan setting per tenant) menempel di `SmtpConfig.bcc` lewat `resolveSmtp()` dan `smtpFromEnv()`, sehingga worker outbox, tombol **Kirim email uji**, dan `bun run mail:test` semuanya menambahkan BCC pada amplop (`RCPT TO` tambahan, tidak di header `To`); email uji menyebutkan alamat BCC di badannya dan di `details` jawaban supaya kotak arsip tahu mengapa ia menerima. (2) Uji SMTP dari Pengaturan sampai kini hanya tercatat di audit: sekarang `recordTestEmail()` (`@core/mail`) menulis baris `smtp-test` ke `outbox_email` sesudah percobaan — `sent` (dengan `sent_at`) atau `failed` (dengan `last_error` = langkah yang gagal), satu percobaan, transport `smtp`, `client_id` = lingkup yang diuji — jadi riwayat uji terlihat di `/outbox` bersama email sungguhan. Pengirimannya sendiri tetap langsung lewat SMTP (bukan antrean), karena jawabannya dibutuhkan saat itu juga. `smtp-test` bukan `TemplateId`: worker merendernya lewat `buildTestMessage()` dengan **SMTP yang berlaku saat itu**, sehingga badan email yang dikirim ulang menyebut server yang benar-benar mengirimnya (tanpa SMTP di luar production: stand-in `log`). (3) `retryOutbox()` hanya menerima baris `failed`; kini `resendOutbox()` + `POST /v1/outbox/:id/resend` (menggantikan `/retry`) mengembalikan baris `failed` **maupun `sent`** (dan `pending` yang sedang menunggu backoff) ke `pending` dengan `attempts = 0`, `next_attempt_at` sekarang, `last_error` kosong; baris `sending` dibiarkan karena sedang dipegang worker (`requeued: 0`, sama seperti id tak dikenal). Halaman `/outbox`: tombol ikon **Kirim ulang** (kunci `outbox.resend`/`outbox.resent` menggantikan `outbox.retry`/`retried`) tampil pada baris terkirim dan gagal, dan **selalu bertanya dulu** (ralat pemilik pada hari yang sama, karena salinan kedua itulah yang dilihat penerima): dipakai `ConfirmDelete` dengan token `confirm=resend` — tanpa JavaScript pemicunya tautan `?confirm=resend&id=<id>` dan barisnya menampilkan pertanyaan inline, dengan JavaScript form yang sama terbuka di modal; form action `?/resend` menolak POST tanpa `?confirm=resend` (`confirmed()`/`confirmFail()`, pesan `common.confirm_required`). Supaya modal itu tetap AJAX, `ConfirmDelete` mendapat prop opsional `enhance` (`SubmitFunction`): bila diberikan, form-nya memakai `use:enhance`, modal ditutup saat submit, dan pemanggil menoast hasilnya lalu `invalidateAll()` — tanpa prop itu perilaku lama (POST biasa) tidak berubah untuk pemakai lain. `apps/web/e2e/outbox.e2e.ts` membuktikan: pemicu membuka dialog, Batal menutupnya tanpa perubahan, konfirmasi menoast dan URL tidak berpindah. Dibuktikan `apps/api/test/integration/mail-test.test.ts` (amplop uji memuat BCC, header `To` tidak; baris `sent` tercatat; port mati → baris `failed` tanpa satu pun amplop; kirim ulang + `deliver` mengirimkannya lewat SMTP yang kini benar, dengan BCC) dan `outbox.test.ts` (kirim ulang baris gagal me-reset percobaan; baris terkirim ikut bisa dikirim ulang dengan penerima/template tetap; baris `sending` dan id asing 0), plus unit `packages/mail/test/smtp-test.test.ts` (`MAIL_BCC` masuk konfigurasi dan badan pesan; alamat rusak ditolak).
50. **Keterlibatan email: dibuka, diklik, ditindaklanjuti** (J-6 baru; diminta pemilik 2026-09-25 setelah bertanya "apakah email sudah dibaca penerima?") — **selesai**: jawabannya "bisa, sebagai sinyal, bukan kepastian", dan ketiga sinyal digabung. Kolom baru di `outbox_email` (migrasi **0029** mysql/pg, **0002** sqlite, semuanya `ALTER … ADD` + indeks unik `open_token` — aditif): `open_token`, `opened_at`, `open_count`, `clicked_at`, `acted_at`. (1) **Pixel**: `GET /v1/mail/o/<token>.gif` (`apps/api/src/domains/mail-track.ts`, publik, tanpa sesi/CSRF) mencatat `opened_at`/`open_count` lalu selalu mengembalikan GIF 1×1 yang sama dengan `Cache-Control: no-store`, juga untuk token tak dikenal. (2) **Klik**: tombol CTA di HTML dibungkus `GET /v1/mail/c/<token>` yang mencatat `clicked_at` (dan `opened_at` bila belum ada) lalu 302 ke `payload.link` baris itu — tujuan tidak pernah dibaca dari request, jadi bukan open redirect; token asing 404. (3) **Ditindaklanjuti**: `markActed()` dipanggil saat token dipakai — `GET /v1/auth/verify-email` (`verify-email`), `POST /v1/auth/reset-password/confirm` (`reset-password` + `set-password`), `POST /v1/auth/join` (`invite` + `invite-existing`) — dan menstempel `acted_at` pada email **terbaru** jenis itu ke alamat itu yang belum distempel; ini satu-satunya sinyal yang tidak bisa dipalsukan proxy gambar. Pixel dan klik **opsional per tenant** lewat setting baru `mail.track_opens` (boolean, baku mati, catatan privasi di deskripsinya); diputuskan di `deliverOutbox()` lewat `opts.tracking` saat pengiriman, jadi berlaku untuk kirim ulang dan untuk setting yang baru dinyalakan, dan `ensureOpenToken()` mencetak token acak 32 hex sekali per baris. `renderTemplate()` menerima parameter kelima `Tracking { pixelUrl, buttonUrl }`: pixel ditempel di akhir body HTML, hanya **tombol** yang dibungkus — tautan cadangan "salin tautan ini" dan versi teks tetap mentah, supaya pembaca yang tidak percaya tautan terlacak tetap punya jalan bersih (token sekali pakainya tetap mencatat kunjungan). Email `smtp-test` **ikut dilacak** (ralat pemilik pada hari yang sama, karena email uji itulah yang pertama dibuka orang untuk mencoba pelacakan): tombol Kirim email uji mencetak token dan menyertakan pixel bila `mail.track_opens` lingkup itu menyala, `recordTestEmail()` menyimpan tokennya, dan `details` jawaban menyebut "pelacakan buka aktif"; kirim ulang dari Outbox merender pixel yang sama lewat `buildTestMessage({ pixelUrl })`. Tidak ada IP/user agent yang disimpan. `/outbox` mendapat kolom **Keterlibatan** (`data-testid="outbox-engagement"`): ditindaklanjuti ✓ > diklik > dibuka ×n, masing-masing dengan waktunya; respons `GET /v1/outbox` membawa `openedAt`, `openCount`, `clickedAt`, `actedAt`. Dibuktikan `apps/api/test/integration/mail-track.test.ts` (setting mati → tanpa token; nyala → token dicetak saat kirim, pixel menghitung 2 kali, token asing GIF sama; klik 302 ke tautan payload + `clicked_at`, token asing/rusak 404; kirim ulang mempertahankan token; verifikasi email menstempel `acted_at` di email terbaru saja; listing memuat keempat field) dan unit `packages/mail/test/tracking.test.ts` (tanpa tracking tidak ada yang berubah; pixel di akhir body, tombol dibungkus, tautan cadangan & teks mentah; email tanpa tautan hanya pixel). **Tanda centang (ralat pemilik pada hari yang sama):** di kolom Status, baris `sent` mendapat satu centang abu-abu (terkirim, belum ada tanda dibuka) atau **dua centang** berwarna `success` bila `acted_at`/`clicked_at`/`opened_at` terisi (`data-testid="outbox-read"`, `data-read`), dengan waktunya di `title`; ikon semantik baru `check-double` didaftarkan di `packages/ui-theme/icons/registry.json` dan dipetakan di ketiga set core (`outline-24`/`bold-24` → lucide `check-check`, `solid-24` → phosphor `Checks`) serta set `rounded-24` milik modul Dummy, karena setiap set wajib menutup seluruh nama terdaftar (L-5).
51. **Status platform di halaman depan bawaan hanya memuat modul yang aktif** (F-6, G-8; diminta pemilik 2026-09-25) — **selesai**: bagian *Status platform* di `/` (dan angka *Modul* di hero-nya) selama ini menampilkan seluruh modul yang terkompilasi dari `GET /v1/version`, termasuk modul yang sudah dinonaktifkan admin — padahal halaman publik modul itu 404 dan menunya hilang (G-8), jadi pengunjung membaca modul yang tidak ada. Kini loader `(public)/+page.server.ts` menyaring daftar itu dengan `locals.config.enabledModules` (ns modul yang aktif untuk tenant pengunjung, dari `GET /v1/module/enabled` — sudah dimuat hooks untuk setiap request), sehingga yang tampil hanya modul **terpasang dan aktif**; bila status modul tidak terbaca, set itu berisi semua modul dan tidak ada yang tersembunyi secara tidak sengaja. Core tetap tidak menyebut nama modul mana pun. Label i18n menyesuaikan: `landing.hero.stat_modules` dan `landing.platform.modules` → "Modul aktif"/"Active modules", `landing.platform.lead` dan `modules_empty` ikut. Gate M4 #4 kini menuntut `<code>example</code>` **tidak** ada di status platform saat Example dinonaktifkan, dan **ada lagi** setelah diaktifkan (dengan `app.landing_route=/` sesaat supaya halaman bawaan bisa dilihat, lalu dikembalikan). Sekalian diperbaiki cek M4 #1 yang usang sejak bahasa baku jadi English (2026-09-18): judul *Kata mereka* / *Paket langganan* kini diminta dengan `accept-language: id` eksplisit.
52. **Penangkap 404 dari konfigurasi** (F-11 baru, §4.7 aturan 6; diminta pemilik 2026-09-25, rencana di [`404-Trapper-Roadmap.md`](./404-Trapper-Roadmap.md)) — **selesai**: toko dan blog memakai URL di akar domain tanpa awalan (`/furniture`, `/kotak-penyimpanan-…`) yang sampai kini pasti 404 karena tidak ada route SvelteKit yang cocok, dan modul tidak punya jalan mengklaimnya (`definePublicRoutes` sengaja menolak `[...slug]`). Kini ada setelan `app.not_found_route` (tipe `public_route`, tepat di bawah *Halaman setelah masuk*, kosong = 404 bawaan) yang menunjuk halaman publik modul; `hooks.server.ts` memeriksa `event.route.id === null` **sebelum** `resolve()` — hanya GET yang meminta HTML, di luar prefix sistem (`NEVER_TRAPPED` di `$lib/server/trap.ts`), tanpa header loop-guard — lalu meneruskannya dengan pola forward landing (`routeTarget()`, generalisasi `landingTarget`) sambil membawa URL asli di header `x-original-path`; jawaban 200/3xx dikembalikan di bawah alamat asli dengan `x-not-found-route`, 404 dari sasaran jatuh diam-diam ke 404 bawaan, 5xx dicatat `warn` lalu 404 bawaan (F-6). Modul membaca URL asli lewat `trappedPath(event)`; layout publik memakainya untuk tujuan kembali form bahasa (juga memperbaiki landing yang selama ini kembali ke `/example`, bukan `/`). Modul `Example` menjadi pemakai rujukan: tabel `example_categories` + kolom `example_products.category_id` (migrasi **0030** mysql/pg, **0004** sqlite, aditif; tanpa FK — batas 64 karakter nama constraint dengan `TABLE_PREFIX`), seed dua kategori, `GET /v1/m/example/resolve?path=` (kategori dulu, lalu produk; path bertingkat/non-slug → 404), `GET /categories`, halaman `/resolve` (kategori: masthead + baris katalog; produk: `ProductDetail.svelte` bersama dengan `/product/[slug]`, canonical URL akar), dan sitemap yang memuat `/<slug>` hanya selama setelan menunjuk `/resolve`. Bukti: unit `trap.test.ts`; integrasi `example.test.ts` (resolve + sitemap bersyarat) dan `config.test.ts` (posisi & tipe field); gate M4 blok F-11 (setelan kosong → 404; `/single-origin` dan `/gayo-arabika` → 200 dari modul dengan canonical akar dan form bahasa kembali ke URL asli; `?query` aman; URL asing → 404 bawaan; `/resolve` langsung → 404; JSON dan `/v1/*` tidak dijebak; `/product/does-not-exist` tetap 404 miliknya; sitemap ikut; modul dimatikan → 404 tanpa loop; setelan dikosongkan → semula). CRUD kategori di dasbor **belum** — seed cukup untuk rujukan (F3 opsional di dokumen rencana).
53. **Halaman 404 dengan adegan robot pencari** (L-19, §4.8; diminta pemilik 2026-09-25) — **selesai**: halaman 404 akar dan 404 di dalam shell dasbor memakai gambar `apps/web/static/404.png` (salinan `docs/screenshot/404.png`: robot pencari yang terkulai, asap, percikan, kaca pembesar) sebagai **latar seluruh layar** (`background-size: cover`, terpusat), dan salinannya **di bagian atas layar** (dipusatkan mendatar, di atas kepala robot sehingga adegannya tetap utuh) — desktop maupun ponsel — di atas panel tembus pandang (`bg-background/65`, blur tipis) agar tetap terbaca di atas robot: `error.root.not_found_title` ("404 Error.") dan `error.root.not_found_lead` (id: "Alamat yang dicari tidak ditemukan, seperti mencari mantan yang hilang."), lewat i18n. Status selain 404 tetap tampilan polos. Dua percobaan sebelumnya ditolak pemilik: ilustrasi SVG isometrik bertoken tema, lalu gambar selebar penuh dengan teks di bawahnya. Diverifikasi lewat `e2e/shots.ts` di 1280 dan 390 px (tanpa overflow).
54. **Tabel aturan trap URL di modul Example — contoh yang bisa dibaca developer lain** (F-11; diminta pemilik 2026-09-25, pembuka jalan modul e-commerce yang merevamp situs lama sambil mempertahankan URL aslinya) — **selesai**: penangkap 404 modul Example kini punya dua resolver berurutan. (1) **Tabel aturan** `modules/Example/trap.ts`: daftar `{ id, pattern, resolve }` yang dicoba dari atas ke bawah atas pathname ternormalkan (`normalizeTrapPath`: decode, tanpa query, tanpa slash akhir, huruf kecil); murni tanpa I/O, diuji unit di `modules/Example/test/trap.test.ts`. Dua aturan demo: `category-prefix` (`/category-food` → "Ini contoh trap URL kategori Food") dan `category-item` (`/food/nasi-goreng` → "Ini contoh trap halaman Nasi Goreng"). (2) **Database** lewat `GET /v1/m/example/resolve` seperti sebelumnya. Halaman demo sengaja **memperlihatkan mekanismenya**: URL yang diminta, aturan yang menjawab, parameter yang diekstrak, status 200 di alamat asli, empat langkah cara kerja, dan deretan URL untuk dicoba (dua dijawab tabel aturan, dua oleh database, satu jatuh ke 404 bawaan); `noindex` karena ini halaman demo. Berkas `trap.ts` dibuka dengan komentar panjang yang menjelaskan alur ujung ke ujung dan alasan tabel aturan — itulah yang disalin modul e-commerce nanti: satu aturan per bentuk URL lama, `resolve` mengembalikan id/slug untuk dicari di tabelnya sendiri. Bukti: unit `trap.test.ts` (urutan aturan, pemaafan slash/kapital/encoding/query, penolakan sisanya, aturan boleh menolak); gate M4 blok F-11 memeriksa kedua URL demo (200, `x-not-found-route`, id aturan dan parameter tampil, `noindex`). MODULES.md sub-bagian penangkap 404 menjelaskan tabel aturan. **Bug yang ketahuan dari URL dua segmen:** SvelteKit menulis tautan aset relatif (`./_app/…`) sehingga halaman yang disajikan di `/food/nasi-goreng` mencari `/food/_app/…` dan tampil tanpa CSS — kini `kit.paths.relative: false` di `svelte.config.js` (aplikasi selalu di akar origin), dijaga cek gate M4 bahwa halaman demo memuat `href="/_app/` dan bukan `./_app/`.
55. **Penangkap 404 memilih modul, bukan sembarang halaman publik** (F-11, E-3; dari uji staging pemilik 2026-09-26: setelan diisi `/example` → semua URL asing jadi landing, padahal ekspektasinya "selain `/category-food` dan `/food/nasi-goreng` tetap 404") — **selesai**: `PublicRouteDef` punya flag `notFound` (titik perluasan 13); `modules:sync` mengalirkannya ke tabel `modulePublicRoutes` (`notFound: boolean`); tipe field baru `not_found_route` (E-3) yang **hanya** menawarkan dan menerima halaman berflag itu dari modul yang aktif (`notFoundRoutesForModules()` di `@core/settings`, dipakai `GET`/`PUT /v1/configuration`), form `/settings` merendernya sebagai pilihan berlabel `Example — /resolve`; `validateValue` menolak halaman lain dengan pesan yang menyebut `notFound: true`; hooks `trap404()` menolak nilai tersimpan yang bukan halaman berflag (dengan `warn`) sehingga nilai lama seperti `/example` tidak lagi mengubah setiap 404 menjadi landing. `/resolve` modul Example diberi `notFound: true`. Bukti: unit `validate.test.ts` (landing ditolak, `/resolve` diterima); integrasi `config.test.ts` (tipe field, `notFoundRoutes` berisi `{ path: '/resolve', module: 'Example' }`, `PUT /example` → 422, `PUT /resolve` → 200); gate M4 blok F-11 tetap hijau; uji staging `deck-staging.qhomeapps.id` dengan `ProxyErrorOverride On 502 503 504`.
56. **Penangkap 404 di galeri `/examples`** (L-19, F-11; diminta pemilik 2026-09-26) — **selesai**: kartu baru *Penangkap 404* di `/examples` menuju `/examples/not-found`, yang menjelaskan fitur ini di instalasi yang sedang berjalan: **keadaan langsung** (penangkap aktif tenant ini dari `app.not_found_route`, atau "404 bawaan"; daftar penangkap yang dideklarasikan modul aktif dari `modulePublicRoutes` berflag `notFound`), tombol ke Pengaturan lingkup Global bagi pemegang `config.edit`, lima langkah cara kerja, deretan URL untuk dicoba (tab baru; dua dijawab tabel aturan, dua oleh database, satu selalu 404), serta cuplikan `public.ts` (`notFound: true`) dan loader penangkap. Hanya membaca, tidak mengubah setelan. Salinan lewat i18n `examples.not_found.*` (id/en). Blok kode diberi `min-w-0` sampai ke kartunya karena `.page` adalah grid dan `<pre>` melebarkan jalurnya di ponsel. Diverifikasi tangkapan layar sebagai admin di 1280 dan 390 px, tanpa overflow.

---

## 9. Cara membaca kemajuan

Jangan mengukur dengan persentase task. Ukur dengan **berapa gate keluar yang sudah hijau** dan **berapa penjaga CI yang sudah menyala**. Keduanya biner, tidak bisa ditawar, dan tidak bisa "90% selesai".

Kalau sebuah milestone lewat dari estimasi lebih dari 50%, hentikan dan periksa: biasanya penyebabnya bukan kecepatan menulis kode, melainkan sebuah kontrak yang belum benar dan sedang ditambal berulang kali. Itu sinyal untuk kembali ke §4.5 atau §4.8, bukan untuk menambah jam kerja.
